# Reconstructed Artifact Metadata

Ideally, JLLs would have always stored their build metadata somewhere canonical.  But they didn't.

Fortunately, most JLLs were built from a known buildsystem in known reproducible environments, allowing
for the retrospective reconstruction of that build metadata. These build metadata are stored here.
