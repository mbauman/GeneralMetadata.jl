dependencies = ["OpenBLAS_jll", "METIS_jll"]
name = "SuiteSparse"
platforms = ["i686-linux-gnu", "x86_64-linux-gnu", "aarch64-linux-gnu", "arm-linux-gnueabihf", "powerpc64le-linux-gnu", "i686-linux-musl", "x86_64-linux-musl", "aarch64-linux-musl", "arm-linux-musleabihf", "x86_64-apple-darwin14", "x86_64-unknown-freebsd11.1", "i686-w64-mingw32", "x86_64-w64-mingw32"]
script = """cd $WORKSPACE/srcdir/SuiteSparse-*\n\n# Apply Jameson's shlib patch\natomic_patch -p1 ${WORKSPACE}/srcdir/patches/SuiteSparse-shlib.patch\n\n# Disable OpenMP as it will probably interfere with blas threads and Julia threads\nFLAGS=(INSTALL=\"${prefix}\" INSTALL_LIB=\"${libdir}\" INSTALL_INCLUDE=\"${prefix}/include\" MY_METIS_LIB=\"-lmetis\" MY_METIS_INC=\"${prefix}/include\" CFOPENMP=)\n\nif [[ ${target} == *mingw32* ]]; then\n    FLAGS+=(UNAME=Windows)\n    FLAGS+=(LDFLAGS=\"${LDFLAGS} -L${libdir} -shared\")\nelse\n    FLAGS+=(UNAME=\"$(uname)\")\n    FLAGS+=(LDFLAGS=\"${LDFLAGS} -L${libdir}\")\nfi\n\nif [[ ${nbits} == 64 ]] && [[ ${target} != aarch64* ]]; then\n    SUN=\"-DSUN64 -DLONGBLAS='long long'\"\n\n    FLAGS+=(BLAS=\"-lopenblas64_\" LAPACK=\"-lopenblas64_\")\n    FLAGS+=(UMFPACK_CONFIG=\"$SUN\" CHOLMOD_CONFIG=\"$SUN\" SPQR_CONFIG=\"$SUN\")\nelse\n    FLAGS+=(BLAS=\"-lopenblas\" LAPACK=\"-lopenblas\")\nfi\n\nmake -j${nproc} -C SuiteSparse_config \"${FLAGS[@]}\" library config\n\nfor proj in SuiteSparse_config AMD BTF CAMD CCOLAMD COLAMD CHOLMOD LDL KLU UMFPACK RBio SPQR; do\n    make -j${nproc} -C $proj \"${FLAGS[@]}\" library CFOPENMP=\"$CFOPENMP\"\n    make -j${nproc} -C $proj \"${FLAGS[@]}\" install CFOPENMP=\"$CFOPENMP\"\ndone\n\n# For now, we'll have to adjust the name of the OpenBLAS library on macOS and FreeBSD.\n# Eventually, this should be fixed upstream\nif [[ ${target} == *-apple-* ]] || [[ ${target} == *freebsd* ]]; then\n    echo \"-- Modifying library name for OpenBLAS\"\n\n    for nm in libcholmod libspqr libumfpack; do\n        # Figure out what version it probably latched on to:\n        if [[ ${target} == *-apple-* ]]; then\n            OPENBLAS_LINK=$(otool -L ${libdir}/${nm}.dylib | grep libopenblas64_ | awk '{ print $1 }')\n            install_name_tool -change ${OPENBLAS_LINK} @rpath/libopenblas64_.dylib ${libdir}/${nm}.dylib\n        elif [[ ${target} == *freebsd* ]]; then\n            OPENBLAS_LINK=$(readelf -d ${libdir}/${nm}.so | grep libopenblas64_ | sed -e 's/.*\\[\\(.*\\)\\].*/\\1/')\n            patchelf --replace-needed ${OPENBLAS_LINK} libopenblas64_.so ${libdir}/${nm}.so\n        fi\n    done\nfi\n\n# Compile SuiteSparse_wrapper shim\ncd $WORKSPACE/srcdir/SuiteSparse_wrapper\n\"${CC}\" -O2 -shared -fPIC -I${prefix}/include SuiteSparse_wrapper.c -o ${libdir}/libsuitesparse_wrapper.${dlext} -L${libdir} -lcholmod\n"""
sources = [{"https://github.com/DrTimothyAldenDavis/SuiteSparse/archive/v5.4.0.tar.gz" = "d9d62d539410d66550d0b795503a556830831f50087723cb191a030525eda770"}, "./bundled"]
version = "v5.4.0"

[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libsuitesparseconfig"]
type = "lib"
variable_name = "libsuitesparseconfig"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libamd"]
type = "lib"
variable_name = "libamd"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libbtf"]
type = "lib"
variable_name = "libbtf"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libcamd"]
type = "lib"
variable_name = "libcamd"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libccolamd"]
type = "lib"
variable_name = "libccolamd"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libcolamd"]
type = "lib"
variable_name = "libcolamd"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libcholmod"]
type = "lib"
variable_name = "libcholmod"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libldl"]
type = "lib"
variable_name = "libldl"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libklu"]
type = "lib"
variable_name = "libklu"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libumfpack"]
type = "lib"
variable_name = "libumfpack"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["librbio"]
type = "lib"
variable_name = "librbio"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libspqr"]
type = "lib"
variable_name = "libspqr"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libsuitesparse_wrapper"]
type = "lib"
variable_name = "libsuitesparse_wrapper"
