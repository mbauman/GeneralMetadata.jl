dependencies = []
init_block = ""
julia_compat = "1.6"
lazy_artifacts = false
name = "Triangle"
platforms = ["i686-linux-gnu", "x86_64-linux-gnu", "aarch64-linux-gnu", "armv7l-linux-gnueabihf", "powerpc64le-linux-gnu", "i686-linux-musl", "x86_64-linux-musl", "aarch64-linux-musl", "armv7l-linux-musleabihf", "x86_64-apple-darwin", "x86_64-unknown-freebsd", "i686-w64-mingw32", "x86_64-w64-mingw32", "aarch64-apple-darwin", "armv6l-linux-gnueabihf", "armv6l-linux-musleabihf"]
script = """cd $WORKSPACE/srcdir/Triangulate.jl/deps/src\nmkdir -p \"${libdir}\"\n\n# Patch the code in order to replace exit() calls in the C code  so that we can\n# throw an error instead.\nsed -e \"s/  exit/extern void error_exit(int); error_exit/g\" triangle/triangle.c > triangle_patched.c\n\n# Concerning the suppression of int - pointer cast warnings,\n# see the following comment in triangle.c:\n#\n# encode() compresses an oriented triangle into a single pointer.  It \n# relies on the assumption that all triangles are aligned to four-byte\n# boundaries, so the two least significant bits of (otri).tri are zero.\n#\n# So there is an inherent hack in the code which we have to allow and hope things keep working.\n$CC -Itriangle  -Wno-int-to-pointer-cast  -Wno-pointer-to-int-cast -DREAL=double -DTRILIBRARY -O3 -fPIC -DNDEBUG -DNO_TIMER -DEXTERNAL_TEST $LDFLAGS --shared -o \"${libdir}/libtriangle.${dlext}\" triangle_patched.c triwrap.c\n\ninstall_license triangle/README\n"""
version = "v1.6.1"

[[products]]
dir_paths = []
dlopen_flags = []
dont_dlopen = false
libnames = ["libtriangle"]
type = "lib"
variable_name = "libtriangle"

[[sources]]
hash = "b2ffb23ca7d89c567fd31367882bd216757cdb9c"
type = "git"
unpack_target = ""
url = "https://github.com/JuliaGeometry/Triangulate.jl.git"
