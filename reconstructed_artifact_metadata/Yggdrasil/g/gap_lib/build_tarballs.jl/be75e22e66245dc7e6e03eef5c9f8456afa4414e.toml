augment_platform_block = ""
init_block = ""
julia_compat = "1.0"
lazy_artifacts = false
name = "GAP_lib"
products = []
script = """cd ${WORKSPACE}/srcdir/gap*\n\nfor f in ${WORKSPACE}/srcdir/patches/*.patch; do\n    atomic_patch -p1 ${f}\ndone\n\nmv ../pkg .\n\n# must run autogen.sh if compiling from git snapshot and/or if configure was patched;\n# it doesn't hurt otherwise, too, so just always do it\n./autogen.sh\n\n# compile a native version of GAP so we can use it to generate the manual\n# (the manual is only in FULL gap release tarballs, not in the -core tarball\n# nor in git snapshots), and also so that we can invoke the\n# `install-gaproot` target (which is arch independent, so no need to use a\n# GAP built for the host arch.)\n./configure --prefix=${prefix} --build=${MACHTYPE} --host=${MACHTYPE} \\\n    --with-gmp=${prefix} \\\n    --without-readline \\\n    --with-zlib=${prefix} \\\n    CC=${CC_BUILD} CXX=${CXX_BUILD}\nmake -j${nproc}\n\n# build the manual if necessary (only HTML and txt; for PDF we'd need LaTeX)\nif [[ ! -f doc/ref/chap0.html ]] ; then\n  make html\nfi\n\n# the license\ninstall_license LICENSE\n\n# install documentation and library files\nmake install-doc install-gaproot\n"""
version = "v400.1201.102"

[[dependencies]]
compat = ""
name = "Zlib_jll"
platforms = ["any"]
top_level = false
type = "builddependency"
version-major = 0
version-minor = 0
version-patch = 0
[[dependencies]]
compat = ""
name = "GMP_jll"
platforms = ["any"]
top_level = false
type = "builddependency"
version-major = 0
version-minor = 0
version-patch = 0

[[sources]]
hash = "1e8e823578e8f1018af592b39bd6f3be1402b482d98f1efb3e24fe6e2f55c926"
type = "archive"
unpack_target = ""
url = "https://github.com/gap-system/gap/releases/download/v4.12.1/gap-4.12.1-core.tar.gz"
[[sources]]
hash = "86d24a1a2208d57822b9aed159b2d5c1306e1a800c6440c6a0d4566e65829c57"
type = "archive"
unpack_target = "pkg"
url = "https://github.com/gap-system/gap/releases/download/v4.12.1/packages-required-v4.12.1.tar.gz"
[[sources]]
follow_symlinks = false
path = "./bundled"
target = ""
type = "directory"
