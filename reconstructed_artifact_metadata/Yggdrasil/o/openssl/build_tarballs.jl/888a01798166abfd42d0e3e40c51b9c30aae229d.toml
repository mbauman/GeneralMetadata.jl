dependencies = []
name = "OpenSSL"
platforms = ["i686-linux-gnu", "x86_64-linux-gnu", "aarch64-linux-gnu", "arm-linux-gnueabihf", "powerpc64le-linux-gnu", "i686-linux-musl", "x86_64-linux-musl", "aarch64-linux-musl", "arm-linux-musleabihf", "x86_64-apple-darwin14", "x86_64-unknown-freebsd11.1", "i686-w64-mingw32", "x86_64-w64-mingw32"]
script = """cd $WORKSPACE/srcdir/openssl-*/\n\n# This build system does not like llvm-ranlib\nif [[ ${target} == *darwin* ]]; then\n    export RANLIB=/opt/${target}/bin/${target}-ranlib\nfi\n\n# Manual translation of BB $target to Configure-target\nfunction translate_target()\n{\n    if [[ ${target} == x86_64-linux* ]]; then\n        echo linux-x86_64\n    elif [[ ${target} == i686-linux* ]]; then\n        echo linux-x86\n    elif [[ ${target} == arm-linux* ]]; then\n        echo linux-armv4\n    elif [[ ${target} == aarch64-linux* ]]; then\n        echo linux-aarch64\n    elif [[ ${target} == powerpc64le-linux* ]]; then\n        echo linux-ppc64le\n    elif [[ ${target} == x86_64-apple-darwin* ]]; then\n        echo darwin64-x86_64-cc\n    elif [[ ${target} == x86_64-unknown-freebsd* ]]; then\n        echo BSD-x86_64\n    elif [[ ${target} == x86_64*mingw* ]]; then\n        echo mingw64\n    elif [[ ${target} == i686*mingw* ]]; then\n        echo mingw\n    else\n        if [[ ${nbits} == 32 ]]; then\n            echo linux-generic32\n        else\n            echo linux-generic64\n        fi\n    fi\n}\n\n./Configure shared --prefix=$prefix $(translate_target)\nmake -j${nproc}\nmake install\n"""
version = "v1.1.1"

[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libcrypto", "libcrypto-1_1", "libcrypto-1_1-x64"]
type = "lib"
variable_name = "libcrypto"
[[products]]
dir_paths = []
dont_dlopen = false
libnames = ["libssl", "libssl-1_1", "libssl-1_1-x64"]
type = "lib"
variable_name = "libssl"
[[products]]
binnames = ["openssl"]
type = "exe"
variable_name = "openssl"

[[sources]]
"https://www.openssl.org/source/openssl-1.1.1c.tar.gz" = "f6fb3079ad15076154eda9413fed42877d668e7069d9b87396d0804fdb3f4c90"
