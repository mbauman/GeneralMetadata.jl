["0.1.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.1.0+0/CUDA_Driver.v0.1.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.1.0+0/CUDA_Driver.v0.1.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.1.0+0/CUDA_Driver.v0.1.0.x86_64-linux-gnu.tar.gz"]
registered = 2022-10-17T10:33:11.000Z

["0.2.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.2.0+0/CUDA_Driver.v0.2.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.2.0+0/CUDA_Driver.v0.2.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.2.0+0/CUDA_Driver.v0.2.0.x86_64-linux-gnu.tar.gz"]
registered = 2022-10-17T14:41:11.000Z

["0.3.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.3.0+0/CUDA_Driver.v0.3.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.3.0+0/CUDA_Driver.v0.3.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.3.0+0/CUDA_Driver.v0.3.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-01-23T13:24:34.000Z

["0.3.0+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.3.0+0/CUDA_Driver.v0.3.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.3.0+0/CUDA_Driver.v0.3.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.3.0+0/CUDA_Driver.v0.3.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-02-03T16:17:43.000Z

["0.4.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.4.0+0/CUDA_Driver.v0.4.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.4.0+0/CUDA_Driver.v0.4.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.4.0+0/CUDA_Driver.v0.4.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-03-10T16:19:32.000Z

["0.4.0+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.4.0+1/CUDA_Driver.v0.4.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.4.0+1/CUDA_Driver.v0.4.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.4.0+1/CUDA_Driver.v0.4.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-03-20T10:36:56.000Z

["0.4.0+2"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.4.0+2/CUDA_Driver.v0.4.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.4.0+2/CUDA_Driver.v0.4.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.4.0+2/CUDA_Driver.v0.4.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-03-20T16:18:01.000Z

["0.5.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.5.0+0/CUDA_Driver.v0.5.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.5.0+0/CUDA_Driver.v0.5.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.5.0+0/CUDA_Driver.v0.5.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-03-24T22:09:17.000Z

["0.5.0+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.5.0+1/CUDA_Driver.v0.5.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.5.0+1/CUDA_Driver.v0.5.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.5.0+1/CUDA_Driver.v0.5.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-04-04T18:23:20.000Z

["0.6.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+0/CUDA_Driver.v0.6.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+0/CUDA_Driver.v0.6.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+0/CUDA_Driver.v0.6.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-08-14T11:35:05.000Z

["0.6.0+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+1/CUDA_Driver.v0.6.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+1/CUDA_Driver.v0.6.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+1/CUDA_Driver.v0.6.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-08-15T12:12:32.000Z

["0.6.0+2"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+2/CUDA_Driver.v0.6.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+2/CUDA_Driver.v0.6.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+2/CUDA_Driver.v0.6.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-08-15T12:32:32.000Z

["0.6.0+3"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+3/CUDA_Driver.v0.6.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+3/CUDA_Driver.v0.6.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+3/CUDA_Driver.v0.6.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-09-01T14:50:50.000Z

["0.6.0+4"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+4/CUDA_Driver.v0.6.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+4/CUDA_Driver.v0.6.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.6.0+4/CUDA_Driver.v0.6.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-10-17T11:03:06.000Z

["0.7.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.7.0+0/CUDA_Driver.v0.7.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.7.0+0/CUDA_Driver.v0.7.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.7.0+0/CUDA_Driver.v0.7.0.x86_64-linux-gnu.tar.gz"]
registered = 2023-10-23T12:25:45.000Z

["0.7.0+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.7.0+1/CUDA_Driver.v0.7.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.7.0+1/CUDA_Driver.v0.7.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.7.0+1/CUDA_Driver.v0.7.0.x86_64-linux-gnu.tar.gz"]
registered = 2024-01-03T10:31:58.000Z

["0.8.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.0+0/CUDA_Driver.v0.8.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.0+0/CUDA_Driver.v0.8.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.0+0/CUDA_Driver.v0.8.0.x86_64-linux-gnu.tar.gz"]
registered = 2024-03-06T09:23:04.000Z

    [["0.8.0+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.0+0/CUDA_Driver.v0.8.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.0+0/CUDA_Driver.v0.8.0.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.0+0/CUDA_Driver.v0.8.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/caa5fc059d4a8d7e74ad513f1586ed2e8bf84bf0/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.5.8"

        ["0.8.0+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    global compat_version = v\"12.4.0\"\n    # global variables we will set\n    global libcuda = nothing\n    global libcuda_version = nothing\n    global libcuda_original_version = nothing\n    # compat_version is set in build_tarballs.jl\n\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    # minimal API call wrappers we need\n    function driver_version(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuDriverGetVersion\"; throw_error=false)\n        if function_handle === nothing\n            @debug \"Driver library seems invalid (does not contain 'cuDriverGetVersion')\"\n            return nothing\n        end\n        version_ref = Ref{Cint}()\n        status = ccall(function_handle, Cint, (Ptr{Cint},), version_ref)\n        if status != 0\n            @debug \"Call to 'cuDriverGetVersion' failed with status $status\"\n            return nothing\n        end\n        major, ver = divrem(version_ref[], 1000)\n        minor, patch = divrem(ver, 10)\n        return VersionNumber(major, minor, patch)\n    end\n    function init_driver(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n        status = ccall(function_handle, Cint, (UInt32,), 0)\n        # libcuda.cuInit dlopens NULL, aka. the main program, which increments the refcount\n        # of libcuda. this breaks future dlclose calls, so eagerly lower the refcount already.\n        Libdl.dlclose(library_handle)\n        return status\n    end\n\n    # find the system driver\n    system_driver = if Sys.iswindows()\n        Libdl.find_library(\"nvcuda\")\n    else\n        Libdl.find_library([\"libcuda.so.1\", \"libcuda.so\"])\n    end\n    if system_driver == \"\"\n        @debug \"No system CUDA driver found\"\n        return\n    end\n    libcuda = system_driver\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    driver_handle = Libdl.dlopen(system_driver; throw_error=false)\n    if driver_handle === nothing\n        @debug \"Failed to load system CUDA driver\"\n        return\n    end\n\n    # query the system driver version\n    # XXX: apparently cuDriverGetVersion can be used before cuInit,\n    #      despite the docs stating \"any function [...] will return\n    #      CUDA_ERROR_NOT_INITIALIZED\"; is this a recent change?\n    system_version = driver_version(driver_handle)\n    if system_version === nothing\n        @debug \"Failed to query system CUDA driver version\"\n        # note that libcuda is already set here, so we'll continue using the system driver\n        # and CUDA.jl will likely report the reason cuDriverGetVersion didn't work.\n        return\n    end\n    @debug \"System CUDA driver found at $system_driver, detected as version $system_version\"\n    libcuda = system_driver\n    libcuda_version = system_version\n\n    # check if the system driver is already loaded (see above)\n    if system_driver_loaded\n        @debug \"System CUDA driver already loaded, continuing using it\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        @debug \"CUDA compat preference: $(compat_preference)\"\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check the version\n    if system_version >= compat_version\n        @debug \"System CUDA driver is recent enough; not using forward-compatible driver\"\n        return\n    end\n\n    # check if we can unload the system driver.\n    # if we didn't, we can't consider a forward compatible library because that would\n    # risk having multiple copies of libcuda.so loaded (also see NVIDIA bug #3418723)\n    Libdl.dlclose(driver_handle)\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    if system_driver_loaded\n        @debug \"Could not unload the system CUDA library;\" *\n               \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if this process is hooked by CUDA's injection libraries, which prevents\n    # unloading libcuda after dlopening. this is problematic, because we might want to\n    # after loading a forwards-compatible libcuda and realizing we can't use it. without\n    # being able to unload the library, we'd run into issues (see NVIDIA bug #3418723)\n    hooked = haskey(ENV, \"CUDA_INJECTION64_PATH\")\n    if hooked\n        @debug \"Running under CUDA injection tools;\" *\n                \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if we even have an artifact\n    if !@isdefined(libcuda_compat)\n        @debug \"No forward-compatible CUDA library available for your platform.\"\n        return\n    end\n    compat_driver = libcuda_compat\n    @debug \"Forward-compatible CUDA driver found at $compat_driver;\" *\n           \" known to be version $(compat_version)\"\n\n    # finally, load the compatibility driver to see if it supports this platform\n    driver_handle = Libdl.dlopen(compat_driver; throw_error=true)\n\n    init_status = init_driver(driver_handle)\n    if init_status != 0\n        @debug \"Could not use forward compatibility package (error $init_status)\"\n\n        # see comment above about unloading the system driver\n        Libdl.dlclose(driver_handle)\n        compat_driver_loaded = Libdl.dlopen(compat_driver, Libdl.RTLD_NOLOAD;\n                                            throw_error=false) !== nothing\n        if compat_driver_loaded\n            error(\"Could not unload forwards compatible CUDA driver.\" *\n                  \"This is probably caused by running Julia under a tool that hooks CUDA API calls.\" *\n                  \"In that case, prevent Julia from loading multiple drivers\" *\n                  \" by setting JULIA_CUDA_USE_COMPAT=false in your environment.\")\n        end\n\n        return\n    end\n\n    # load dependent libraries\n    # XXX: we can do this after loading libcuda, because these are runtime dependencies.\n    #      if loading libcuda or calling cuInit would already require these, do so earlier.\n    Libdl.dlopen(libcuda_debugger; throw_error=true)\n    Libdl.dlopen(libnvidia_nvvm; throw_error=true)\n    Libdl.dlopen(libnvidia_ptxjitcompiler; throw_error=true)\n\n    @debug \"Successfully loaded forwards-compatible CUDA driver\"\n    libcuda = compat_driver\n    libcuda_version = compat_version\n    libcuda_original_version = system_version\n"""
        julia_compat = "1.0"
        lazy_artifacts = true
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "powerpc64le-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.8.0"

            [["0.8.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.8.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.8.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.8.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.8.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "d2d3425133556ec9501de52aae9c858e08a4ae454e7321e0035651032c088cfc"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-4-550.54.14-1.x86_64.rpm"
            [["0.8.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "4ecb6162fa1216455d67d0cafeb1f443fc603bb32cbf77d928ace931b5f362dd"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/ppc64le/cuda-compat-12-4-550.54.14-1.ppc64le.rpm"
            [["0.8.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "f6add6a10c3ee381db97594185d847df051ca9616f223296b118788b51682a01"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-4-550.54.14-1.aarch64.rpm"

["0.8.1+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.1+0/CUDA_Driver.v0.8.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.1+0/CUDA_Driver.v0.8.1.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.1+0/CUDA_Driver.v0.8.1.x86_64-linux-gnu.tar.gz"]
registered = 2024-04-11T14:32:55.000Z

    [["0.8.1+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.1+0/CUDA_Driver.v0.8.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.1+0/CUDA_Driver.v0.8.1.powerpc64le-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.8.1+0/CUDA_Driver.v0.8.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/1abb48b27c1d9ebb9aa5e677dbd92614116a4b48/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.5.8"

        ["0.8.1+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    global compat_version = v\"12.4.0\"\n    # global variables we will set\n    global libcuda = nothing\n    global libcuda_version = nothing\n    global libcuda_original_version = nothing\n    # compat_version is set in build_tarballs.jl\n\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    # minimal API call wrappers we need\n    function driver_version(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuDriverGetVersion\"; throw_error=false)\n        if function_handle === nothing\n            @debug \"Driver library seems invalid (does not contain 'cuDriverGetVersion')\"\n            return nothing\n        end\n        version_ref = Ref{Cint}()\n        status = ccall(function_handle, Cint, (Ptr{Cint},), version_ref)\n        if status != 0\n            @debug \"Call to 'cuDriverGetVersion' failed with status $status\"\n            return nothing\n        end\n        major, ver = divrem(version_ref[], 1000)\n        minor, patch = divrem(ver, 10)\n        return VersionNumber(major, minor, patch)\n    end\n    function init_driver(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n        status = ccall(function_handle, Cint, (UInt32,), 0)\n        # libcuda.cuInit dlopens NULL, aka. the main program, which increments the refcount\n        # of libcuda. this breaks future dlclose calls, so eagerly lower the refcount already.\n        Libdl.dlclose(library_handle)\n        return status\n    end\n\n    # find the system driver\n    system_driver = if Sys.iswindows()\n        Libdl.find_library(\"nvcuda\")\n    else\n        Libdl.find_library([\"libcuda.so.1\", \"libcuda.so\"])\n    end\n    if system_driver == \"\"\n        @debug \"No system CUDA driver found\"\n        return\n    end\n    libcuda = system_driver\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    driver_handle = Libdl.dlopen(system_driver; throw_error=false)\n    if driver_handle === nothing\n        @debug \"Failed to load system CUDA driver\"\n        return\n    end\n\n    # query the system driver version\n    # XXX: apparently cuDriverGetVersion can be used before cuInit,\n    #      despite the docs stating \"any function [...] will return\n    #      CUDA_ERROR_NOT_INITIALIZED\"; is this a recent change?\n    system_version = driver_version(driver_handle)\n    if system_version === nothing\n        @debug \"Failed to query system CUDA driver version\"\n        # note that libcuda is already set here, so we'll continue using the system driver\n        # and CUDA.jl will likely report the reason cuDriverGetVersion didn't work.\n        return\n    end\n    @debug \"System CUDA driver found at $system_driver, detected as version $system_version\"\n    libcuda = system_driver\n    libcuda_version = system_version\n\n    # check if the system driver is already loaded (see above)\n    if system_driver_loaded\n        @debug \"System CUDA driver already loaded, continuing using it\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        @debug \"CUDA compat preference: $(compat_preference)\"\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check the version\n    if system_version >= compat_version\n        @debug \"System CUDA driver is recent enough; not using forward-compatible driver\"\n        return\n    end\n\n    # check if we can unload the system driver.\n    # if we didn't, we can't consider a forward compatible library because that would\n    # risk having multiple copies of libcuda.so loaded (also see NVIDIA bug #3418723)\n    Libdl.dlclose(driver_handle)\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    if system_driver_loaded\n        @debug \"Could not unload the system CUDA library;\" *\n               \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if this process is hooked by CUDA's injection libraries, which prevents\n    # unloading libcuda after dlopening. this is problematic, because we might want to\n    # after loading a forwards-compatible libcuda and realizing we can't use it. without\n    # being able to unload the library, we'd run into issues (see NVIDIA bug #3418723)\n    hooked = haskey(ENV, \"CUDA_INJECTION64_PATH\")\n    if hooked\n        @debug \"Running under CUDA injection tools;\" *\n                \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if we even have an artifact\n    if !@isdefined(libcuda_compat)\n        @debug \"No forward-compatible CUDA library available for your platform.\"\n        return\n    end\n    compat_driver = libcuda_compat\n    @debug \"Forward-compatible CUDA driver found at $compat_driver;\" *\n           \" known to be version $(compat_version)\"\n\n    # finally, load the compatibility driver to see if it supports this platform\n    driver_handle = Libdl.dlopen(compat_driver; throw_error=true)\n\n    init_status = init_driver(driver_handle)\n    if init_status != 0\n        @debug \"Could not use forward compatibility package (error $init_status)\"\n\n        # see comment above about unloading the system driver\n        Libdl.dlclose(driver_handle)\n        compat_driver_loaded = Libdl.dlopen(compat_driver, Libdl.RTLD_NOLOAD;\n                                            throw_error=false) !== nothing\n        if compat_driver_loaded\n            error(\"Could not unload forwards compatible CUDA driver.\" *\n                  \"This is probably caused by running Julia under a tool that hooks CUDA API calls.\" *\n                  \"In that case, prevent Julia from loading multiple drivers\" *\n                  \" by setting JULIA_CUDA_USE_COMPAT=false in your environment.\")\n        end\n\n        return\n    end\n\n    # load dependent libraries\n    # XXX: we can do this after loading libcuda, because these are runtime dependencies.\n    #      if loading libcuda or calling cuInit would already require these, do so earlier.\n    Libdl.dlopen(libcuda_debugger; throw_error=true)\n    Libdl.dlopen(libnvidia_nvvm; throw_error=true)\n    Libdl.dlopen(libnvidia_ptxjitcompiler; throw_error=true)\n\n    @debug \"Successfully loaded forwards-compatible CUDA driver\"\n    libcuda = compat_driver\n    libcuda_version = compat_version\n    libcuda_original_version = system_version\n"""
        julia_compat = "1.0"
        lazy_artifacts = true
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "powerpc64le-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.8.1"

            [["0.8.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.8.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.8.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.8.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.8.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "3e2727fb76285978ac4a1e0c8b7ca5d88ece4bf124dfb2a3d0a486829bf315d3"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-4-550.54.15-1.x86_64.rpm"
            [["0.8.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "1d487161589b3d91a3648ca271046fa451072ceddb070e4aaeb7ed377d990b98"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/ppc64le/cuda-compat-12-4-550.54.15-1.ppc64le.rpm"
            [["0.8.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "bafe0831dd92a5ba47d790709f0d642d4f30cf2ade5d32371d012ae09b5c2512"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-4-550.54.15-1.aarch64.rpm"

["0.9.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.0+0/CUDA_Driver.v0.9.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.0+0/CUDA_Driver.v0.9.0.x86_64-linux-gnu.tar.gz"]
registered = 2024-05-22T08:37:02.000Z

    [["0.9.0+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.0+0/CUDA_Driver.v0.9.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.0+0/CUDA_Driver.v0.9.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/fc8fe21caae2a45c43ad122ca342a96fdbc63af0/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.5.8"

        ["0.9.0+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    global compat_version = v\"12.5.0\"\n    # global variables we will set\n    global libcuda = nothing\n    global libcuda_version = nothing\n    global libcuda_original_version = nothing\n    # compat_version is set in build_tarballs.jl\n\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    # minimal API call wrappers we need\n    function driver_version(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuDriverGetVersion\"; throw_error=false)\n        if function_handle === nothing\n            @debug \"Driver library seems invalid (does not contain 'cuDriverGetVersion')\"\n            return nothing\n        end\n        version_ref = Ref{Cint}()\n        status = ccall(function_handle, Cint, (Ptr{Cint},), version_ref)\n        if status != 0\n            @debug \"Call to 'cuDriverGetVersion' failed with status $status\"\n            return nothing\n        end\n        major, ver = divrem(version_ref[], 1000)\n        minor, patch = divrem(ver, 10)\n        return VersionNumber(major, minor, patch)\n    end\n    function init_driver(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n        status = ccall(function_handle, Cint, (UInt32,), 0)\n        # libcuda.cuInit dlopens NULL, aka. the main program, which increments the refcount\n        # of libcuda. this breaks future dlclose calls, so eagerly lower the refcount already.\n        Libdl.dlclose(library_handle)\n        return status\n    end\n\n    # find the system driver\n    system_driver = if Sys.iswindows()\n        Libdl.find_library(\"nvcuda\")\n    else\n        Libdl.find_library([\"libcuda.so.1\", \"libcuda.so\"])\n    end\n    if system_driver == \"\"\n        @debug \"No system CUDA driver found\"\n        return\n    end\n    libcuda = system_driver\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    driver_handle = Libdl.dlopen(system_driver; throw_error=false)\n    if driver_handle === nothing\n        @debug \"Failed to load system CUDA driver\"\n        return\n    end\n\n    # query the system driver version\n    # XXX: apparently cuDriverGetVersion can be used before cuInit,\n    #      despite the docs stating \"any function [...] will return\n    #      CUDA_ERROR_NOT_INITIALIZED\"; is this a recent change?\n    system_version = driver_version(driver_handle)\n    if system_version === nothing\n        @debug \"Failed to query system CUDA driver version\"\n        # note that libcuda is already set here, so we'll continue using the system driver\n        # and CUDA.jl will likely report the reason cuDriverGetVersion didn't work.\n        return\n    end\n    @debug \"System CUDA driver found at $system_driver, detected as version $system_version\"\n    libcuda = system_driver\n    libcuda_version = system_version\n\n    # check if the system driver is already loaded (see above)\n    if system_driver_loaded\n        @debug \"System CUDA driver already loaded, continuing using it\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        @debug \"CUDA compat preference: $(compat_preference)\"\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check the version\n    if system_version >= compat_version\n        @debug \"System CUDA driver is recent enough; not using forward-compatible driver\"\n        return\n    end\n\n    # check if we can unload the system driver.\n    # if we didn't, we can't consider a forward compatible library because that would\n    # risk having multiple copies of libcuda.so loaded (also see NVIDIA bug #3418723)\n    Libdl.dlclose(driver_handle)\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    if system_driver_loaded\n        @debug \"Could not unload the system CUDA library;\" *\n               \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if this process is hooked by CUDA's injection libraries, which prevents\n    # unloading libcuda after dlopening. this is problematic, because we might want to\n    # after loading a forwards-compatible libcuda and realizing we can't use it. without\n    # being able to unload the library, we'd run into issues (see NVIDIA bug #3418723)\n    hooked = haskey(ENV, \"CUDA_INJECTION64_PATH\")\n    if hooked\n        @debug \"Running under CUDA injection tools;\" *\n                \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if we even have an artifact\n    if !@isdefined(libcuda_compat)\n        @debug \"No forward-compatible CUDA library available for your platform.\"\n        return\n    end\n    compat_driver = libcuda_compat\n    @debug \"Forward-compatible CUDA driver found at $compat_driver;\" *\n           \" known to be version $(compat_version)\"\n\n    # finally, load the compatibility driver to see if it supports this platform\n    driver_handle = Libdl.dlopen(compat_driver; throw_error=true)\n\n    init_status = init_driver(driver_handle)\n    if init_status != 0\n        @debug \"Could not use forward compatibility package (error $init_status)\"\n\n        # see comment above about unloading the system driver\n        Libdl.dlclose(driver_handle)\n        compat_driver_loaded = Libdl.dlopen(compat_driver, Libdl.RTLD_NOLOAD;\n                                            throw_error=false) !== nothing\n        if compat_driver_loaded\n            error(\"Could not unload forwards compatible CUDA driver.\" *\n                  \"This is probably caused by running Julia under a tool that hooks CUDA API calls.\" *\n                  \"In that case, prevent Julia from loading multiple drivers\" *\n                  \" by setting JULIA_CUDA_USE_COMPAT=false in your environment.\")\n        end\n\n        return\n    end\n\n    # load dependent libraries\n    # XXX: we can do this after loading libcuda, because these are runtime dependencies.\n    #      if loading libcuda or calling cuInit would already require these, do so earlier.\n    Libdl.dlopen(libcuda_debugger; throw_error=true)\n    Libdl.dlopen(libnvidia_nvvm; throw_error=true)\n    Libdl.dlopen(libnvidia_ptxjitcompiler; throw_error=true)\n\n    @debug \"Successfully loaded forwards-compatible CUDA driver\"\n    libcuda = compat_driver\n    libcuda_version = compat_version\n    libcuda_original_version = system_version\n"""
        julia_compat = "1.0"
        lazy_artifacts = true
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.9.0"

            [["0.9.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.9.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.9.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.9.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.9.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "b5f70dfd2149ef41a3decda1a398b805430d7129e487f17e9a4f154384be660e"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-5-555.42.02-1.x86_64.rpm"
            [["0.9.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "bce9fe958f8e0811bb5cc40cc0235de8453c6a4e89b12e8c0458d78768af908a"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-5-555.42.02-1.aarch64.rpm"

["0.9.1+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.1+0/CUDA_Driver.v0.9.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.1+0/CUDA_Driver.v0.9.1.x86_64-linux-gnu.tar.gz"]
registered = 2024-07-03T19:25:32.000Z

    [["0.9.1+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.1+0/CUDA_Driver.v0.9.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.1+0/CUDA_Driver.v0.9.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/096e4c2516fea296f70a7cfc5f95d940242c87d9/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.5.8"

        ["0.9.1+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    global compat_version = v\"12.5.0\"\n    # global variables we will set\n    global libcuda = nothing\n    global libcuda_version = nothing\n    global libcuda_original_version = nothing\n    # compat_version is set in build_tarballs.jl\n\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    # minimal API call wrappers we need\n    function driver_version(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuDriverGetVersion\"; throw_error=false)\n        if function_handle === nothing\n            @debug \"Driver library seems invalid (does not contain 'cuDriverGetVersion')\"\n            return nothing\n        end\n        version_ref = Ref{Cint}()\n        status = ccall(function_handle, Cint, (Ptr{Cint},), version_ref)\n        if status != 0\n            @debug \"Call to 'cuDriverGetVersion' failed with status $status\"\n            return nothing\n        end\n        major, ver = divrem(version_ref[], 1000)\n        minor, patch = divrem(ver, 10)\n        return VersionNumber(major, minor, patch)\n    end\n    function init_driver(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n        status = ccall(function_handle, Cint, (UInt32,), 0)\n        # libcuda.cuInit dlopens NULL, aka. the main program, which increments the refcount\n        # of libcuda. this breaks future dlclose calls, so eagerly lower the refcount already.\n        Libdl.dlclose(library_handle)\n        return status\n    end\n\n    # find the system driver\n    system_driver = if Sys.iswindows()\n        Libdl.find_library(\"nvcuda\")\n    else\n        Libdl.find_library([\"libcuda.so.1\", \"libcuda.so\"])\n    end\n    if system_driver == \"\"\n        @debug \"No system CUDA driver found\"\n        return\n    end\n    libcuda = system_driver\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    driver_handle = Libdl.dlopen(system_driver; throw_error=false)\n    if driver_handle === nothing\n        @debug \"Failed to load system CUDA driver\"\n        return\n    end\n\n    # query the system driver version\n    # XXX: apparently cuDriverGetVersion can be used before cuInit,\n    #      despite the docs stating \"any function [...] will return\n    #      CUDA_ERROR_NOT_INITIALIZED\"; is this a recent change?\n    system_version = driver_version(driver_handle)\n    if system_version === nothing\n        @debug \"Failed to query system CUDA driver version\"\n        # note that libcuda is already set here, so we'll continue using the system driver\n        # and CUDA.jl will likely report the reason cuDriverGetVersion didn't work.\n        return\n    end\n    @debug \"System CUDA driver found at $system_driver, detected as version $system_version\"\n    libcuda = system_driver\n    libcuda_version = system_version\n\n    # check if the system driver is already loaded (see above)\n    if system_driver_loaded\n        @debug \"System CUDA driver already loaded, continuing using it\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        @debug \"CUDA compat preference: $(compat_preference)\"\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check the version\n    if system_version >= compat_version\n        @debug \"System CUDA driver is recent enough; not using forward-compatible driver\"\n        return\n    end\n\n    # check if we can unload the system driver.\n    # if we didn't, we can't consider a forward compatible library because that would\n    # risk having multiple copies of libcuda.so loaded (also see NVIDIA bug #3418723)\n    Libdl.dlclose(driver_handle)\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    if system_driver_loaded\n        @debug \"Could not unload the system CUDA library;\" *\n               \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if this process is hooked by CUDA's injection libraries, which prevents\n    # unloading libcuda after dlopening. this is problematic, because we might want to\n    # after loading a forwards-compatible libcuda and realizing we can't use it. without\n    # being able to unload the library, we'd run into issues (see NVIDIA bug #3418723)\n    hooked = haskey(ENV, \"CUDA_INJECTION64_PATH\")\n    if hooked\n        @debug \"Running under CUDA injection tools;\" *\n                \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if we even have an artifact\n    if !@isdefined(libcuda_compat)\n        @debug \"No forward-compatible CUDA library available for your platform.\"\n        return\n    end\n    compat_driver = libcuda_compat\n    @debug \"Forward-compatible CUDA driver found at $compat_driver;\" *\n           \" known to be version $(compat_version)\"\n\n    # finally, load the compatibility driver to see if it supports this platform\n    driver_handle = Libdl.dlopen(compat_driver; throw_error=true)\n\n    init_status = init_driver(driver_handle)\n    if init_status != 0\n        @debug \"Could not use forward compatibility package (error $init_status)\"\n\n        # see comment above about unloading the system driver\n        Libdl.dlclose(driver_handle)\n        compat_driver_loaded = Libdl.dlopen(compat_driver, Libdl.RTLD_NOLOAD;\n                                            throw_error=false) !== nothing\n        if compat_driver_loaded\n            error(\"Could not unload forwards compatible CUDA driver.\" *\n                  \"This is probably caused by running Julia under a tool that hooks CUDA API calls.\" *\n                  \"In that case, prevent Julia from loading multiple drivers\" *\n                  \" by setting JULIA_CUDA_USE_COMPAT=false in your environment.\")\n        end\n\n        return\n    end\n\n    # load dependent libraries\n    # XXX: we can do this after loading libcuda, because these are runtime dependencies.\n    #      if loading libcuda or calling cuInit would already require these, do so earlier.\n    Libdl.dlopen(libcuda_debugger; throw_error=true)\n    Libdl.dlopen(libnvidia_nvvm; throw_error=true)\n    Libdl.dlopen(libnvidia_ptxjitcompiler; throw_error=true)\n\n    @debug \"Successfully loaded forwards-compatible CUDA driver\"\n    libcuda = compat_driver\n    libcuda_version = compat_version\n    libcuda_original_version = system_version\n"""
        julia_compat = "1.0"
        lazy_artifacts = true
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.9.1"

            [["0.9.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.9.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.9.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.9.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.9.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "66d63219c926733ecd641a709d98a5cb7406712af000d141d0c23d08e0bdb88b"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-5-555.42.06-1.x86_64.rpm"
            [["0.9.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "600d86e143b9fa97ec7862ff634210cacf072ce709c738ee322c932b763ab138"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-5-555.42.06-1.aarch64.rpm"

["0.9.1+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.1+1/CUDA_Driver.v0.9.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.1+1/CUDA_Driver.v0.9.1.x86_64-linux-gnu.tar.gz"]
registered = 2024-07-04T14:07:31.000Z

    [["0.9.1+1".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.1+1/CUDA_Driver.v0.9.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.1+1/CUDA_Driver.v0.9.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/f7f969d70b4cb30fec7396057a77e0b6777214ea/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.5.8"

        ["0.9.1+1".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    global compat_version = v\"12.5.0\"\n    # global variables we will set\n    global libcuda = nothing\n    global libcuda_version = nothing\n    global libcuda_original_version = nothing\n    # compat_version is set in build_tarballs.jl\n\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    # minimal API call wrappers we need\n    function driver_version(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuDriverGetVersion\"; throw_error=false)\n        if function_handle === nothing\n            @debug \"Driver library seems invalid (does not contain 'cuDriverGetVersion')\"\n            return nothing\n        end\n        version_ref = Ref{Cint}()\n        status = ccall(function_handle, Cint, (Ptr{Cint},), version_ref)\n        if status != 0\n            @debug \"Call to 'cuDriverGetVersion' failed with status $status\"\n            return nothing\n        end\n        major, ver = divrem(version_ref[], 1000)\n        minor, patch = divrem(ver, 10)\n        return VersionNumber(major, minor, patch)\n    end\n    function init_driver(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n        status = ccall(function_handle, Cint, (UInt32,), 0)\n        # libcuda.cuInit dlopens NULL, aka. the main program, which increments the refcount\n        # of libcuda. this breaks future dlclose calls, so eagerly lower the refcount already.\n        Libdl.dlclose(library_handle)\n        return status\n    end\n\n    # find the system driver\n    system_driver = if Sys.iswindows()\n        Libdl.find_library(\"nvcuda\")\n    else\n        Libdl.find_library([\"libcuda.so.1\", \"libcuda.so\"])\n    end\n    if system_driver == \"\"\n        @debug \"No system CUDA driver found\"\n        return\n    end\n    libcuda = system_driver\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    driver_handle = Libdl.dlopen(system_driver; throw_error=false)\n    if driver_handle === nothing\n        @debug \"Failed to load system CUDA driver\"\n        return\n    end\n\n    # query the system driver version\n    # XXX: apparently cuDriverGetVersion can be used before cuInit,\n    #      despite the docs stating \"any function [...] will return\n    #      CUDA_ERROR_NOT_INITIALIZED\"; is this a recent change?\n    system_version = driver_version(driver_handle)\n    if system_version === nothing\n        @debug \"Failed to query system CUDA driver version\"\n        # note that libcuda is already set here, so we'll continue using the system driver\n        # and CUDA.jl will likely report the reason cuDriverGetVersion didn't work.\n        return\n    end\n    @debug \"System CUDA driver found at $system_driver, detected as version $system_version\"\n    libcuda = system_driver\n    libcuda_version = system_version\n\n    # check if the system driver is already loaded (see above)\n    if system_driver_loaded\n        @debug \"System CUDA driver already loaded, continuing using it\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        @debug \"CUDA compat preference: $(compat_preference)\"\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check the version\n    if system_version >= compat_version\n        @debug \"System CUDA driver is recent enough; not using forward-compatible driver\"\n        return\n    end\n\n    # check if we can unload the system driver.\n    # if we didn't, we can't consider a forward compatible library because that would\n    # risk having multiple copies of libcuda.so loaded (also see NVIDIA bug #3418723)\n    Libdl.dlclose(driver_handle)\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    if system_driver_loaded\n        @debug \"Could not unload the system CUDA library;\" *\n               \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if this process is hooked by CUDA's injection libraries, which prevents\n    # unloading libcuda after dlopening. this is problematic, because we might want to\n    # after loading a forwards-compatible libcuda and realizing we can't use it. without\n    # being able to unload the library, we'd run into issues (see NVIDIA bug #3418723)\n    hooked = haskey(ENV, \"CUDA_INJECTION64_PATH\")\n    if hooked\n        @debug \"Running under CUDA injection tools;\" *\n                \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if we even have an artifact\n    if !@isdefined(libcuda_compat)\n        @debug \"No forward-compatible CUDA library available for your platform.\"\n        return\n    end\n    compat_driver = libcuda_compat\n    @debug \"Forward-compatible CUDA driver found at $compat_driver;\" *\n           \" known to be version $(compat_version)\"\n\n    # finally, load the compatibility driver to see if it supports this platform\n    driver_handle = Libdl.dlopen(compat_driver; throw_error=true)\n\n    init_status = init_driver(driver_handle)\n    if init_status != 0\n        @debug \"Could not use forward compatibility package (error $init_status)\"\n\n        # see comment above about unloading the system driver\n        Libdl.dlclose(driver_handle)\n        compat_driver_loaded = Libdl.dlopen(compat_driver, Libdl.RTLD_NOLOAD;\n                                            throw_error=false) !== nothing\n        if compat_driver_loaded\n            error(\"Could not unload forwards compatible CUDA driver.\" *\n                  \"This is probably caused by running Julia under a tool that hooks CUDA API calls.\" *\n                  \"In that case, prevent Julia from loading multiple drivers\" *\n                  \" by setting JULIA_CUDA_USE_COMPAT=false in your environment.\")\n        end\n\n        return\n    end\n\n    # load dependent libraries\n    # XXX: we can do this after loading libcuda, because these are runtime dependencies.\n    #      if loading libcuda or calling cuInit would already require these, do so earlier.\n    Libdl.dlopen(libcuda_debugger; throw_error=true)\n    Libdl.dlopen(libnvidia_nvvm; throw_error=true)\n    Libdl.dlopen(libnvidia_ptxjitcompiler; throw_error=true)\n\n    @debug \"Successfully loaded forwards-compatible CUDA driver\"\n    libcuda = compat_driver\n    libcuda_version = compat_version\n    libcuda_original_version = system_version\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.9.1"

            [["0.9.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.9.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.9.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.9.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.9.1+1".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "66d63219c926733ecd641a709d98a5cb7406712af000d141d0c23d08e0bdb88b"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-5-555.42.06-1.x86_64.rpm"
            [["0.9.1+1".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "600d86e143b9fa97ec7862ff634210cacf072ce709c738ee322c932b763ab138"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-5-555.42.06-1.aarch64.rpm"

["0.9.2+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.2+0/CUDA_Driver.v0.9.2.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.2+0/CUDA_Driver.v0.9.2.x86_64-linux-gnu.tar.gz"]
registered = 2024-08-06T14:29:32.000Z

    [["0.9.2+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.2+0/CUDA_Driver.v0.9.2.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.9.2+0/CUDA_Driver.v0.9.2.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/aaebeab926b947eb3f8874887ba6ec1b3bb2d37c/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.5.8"

        ["0.9.2+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    global compat_version = v\"12.6.0\"\n    # global variables we will set\n    global libcuda = nothing\n    global libcuda_version = nothing\n    global libcuda_original_version = nothing\n    # compat_version is set in build_tarballs.jl\n\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    # minimal API call wrappers we need\n    function driver_version(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuDriverGetVersion\"; throw_error=false)\n        if function_handle === nothing\n            @debug \"Driver library seems invalid (does not contain 'cuDriverGetVersion')\"\n            return nothing\n        end\n        version_ref = Ref{Cint}()\n        status = ccall(function_handle, Cint, (Ptr{Cint},), version_ref)\n        if status != 0\n            @debug \"Call to 'cuDriverGetVersion' failed with status $status\"\n            return nothing\n        end\n        major, ver = divrem(version_ref[], 1000)\n        minor, patch = divrem(ver, 10)\n        return VersionNumber(major, minor, patch)\n    end\n    function init_driver(library_handle)\n        function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n        status = ccall(function_handle, Cint, (UInt32,), 0)\n        # libcuda.cuInit dlopens NULL, aka. the main program, which increments the refcount\n        # of libcuda. this breaks future dlclose calls, so eagerly lower the refcount already.\n        Libdl.dlclose(library_handle)\n        return status\n    end\n\n    # find the system driver\n    system_driver = if Sys.iswindows()\n        Libdl.find_library(\"nvcuda\")\n    else\n        Libdl.find_library([\"libcuda.so.1\", \"libcuda.so\"])\n    end\n    if system_driver == \"\"\n        @debug \"No system CUDA driver found\"\n        return\n    end\n    libcuda = system_driver\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    driver_handle = Libdl.dlopen(system_driver; throw_error=false)\n    if driver_handle === nothing\n        @debug \"Failed to load system CUDA driver\"\n        return\n    end\n\n    # query the system driver version\n    # XXX: apparently cuDriverGetVersion can be used before cuInit,\n    #      despite the docs stating \"any function [...] will return\n    #      CUDA_ERROR_NOT_INITIALIZED\"; is this a recent change?\n    system_version = driver_version(driver_handle)\n    if system_version === nothing\n        @debug \"Failed to query system CUDA driver version\"\n        # note that libcuda is already set here, so we'll continue using the system driver\n        # and CUDA.jl will likely report the reason cuDriverGetVersion didn't work.\n        return\n    end\n    @debug \"System CUDA driver found at $system_driver, detected as version $system_version\"\n    libcuda = system_driver\n    libcuda_version = system_version\n\n    # check if the system driver is already loaded (see above)\n    if system_driver_loaded\n        @debug \"System CUDA driver already loaded, continuing using it\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        @debug \"CUDA compat preference: $(compat_preference)\"\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check the version\n    if system_version >= compat_version\n        @debug \"System CUDA driver is recent enough; not using forward-compatible driver\"\n        return\n    end\n\n    # check if we can unload the system driver.\n    # if we didn't, we can't consider a forward compatible library because that would\n    # risk having multiple copies of libcuda.so loaded (also see NVIDIA bug #3418723)\n    Libdl.dlclose(driver_handle)\n    system_driver_loaded = Libdl.dlopen(system_driver, Libdl.RTLD_NOLOAD;\n                                        throw_error=false) !== nothing\n    if system_driver_loaded\n        @debug \"Could not unload the system CUDA library;\" *\n               \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if this process is hooked by CUDA's injection libraries, which prevents\n    # unloading libcuda after dlopening. this is problematic, because we might want to\n    # after loading a forwards-compatible libcuda and realizing we can't use it. without\n    # being able to unload the library, we'd run into issues (see NVIDIA bug #3418723)\n    hooked = haskey(ENV, \"CUDA_INJECTION64_PATH\")\n    if hooked\n        @debug \"Running under CUDA injection tools;\" *\n                \" this prevents use of the forward-compatible driver\"\n        return\n    end\n\n    # check if we even have an artifact\n    if !@isdefined(libcuda_compat)\n        @debug \"No forward-compatible CUDA library available for your platform.\"\n        return\n    end\n    compat_driver = libcuda_compat\n    @debug \"Forward-compatible CUDA driver found at $compat_driver;\" *\n           \" known to be version $(compat_version)\"\n\n    # finally, load the compatibility driver to see if it supports this platform\n    driver_handle = Libdl.dlopen(compat_driver; throw_error=true)\n\n    init_status = init_driver(driver_handle)\n    if init_status != 0\n        @debug \"Could not use forward compatibility package (error $init_status)\"\n\n        # see comment above about unloading the system driver\n        Libdl.dlclose(driver_handle)\n        compat_driver_loaded = Libdl.dlopen(compat_driver, Libdl.RTLD_NOLOAD;\n                                            throw_error=false) !== nothing\n        if compat_driver_loaded\n            error(\"Could not unload forwards compatible CUDA driver.\" *\n                  \"This is probably caused by running Julia under a tool that hooks CUDA API calls.\" *\n                  \"In that case, prevent Julia from loading multiple drivers\" *\n                  \" by setting JULIA_CUDA_USE_COMPAT=false in your environment.\")\n        end\n\n        return\n    end\n\n    # load dependent libraries\n    # XXX: we can do this after loading libcuda, because these are runtime dependencies.\n    #      if loading libcuda or calling cuInit would already require these, do so earlier.\n    Libdl.dlopen(libcuda_debugger; throw_error=true)\n    Libdl.dlopen(libnvidia_nvvm; throw_error=true)\n    Libdl.dlopen(libnvidia_ptxjitcompiler; throw_error=true)\n\n    @debug \"Successfully loaded forwards-compatible CUDA driver\"\n    libcuda = compat_driver\n    libcuda_version = compat_version\n    libcuda_original_version = system_version\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.9.2"

            [["0.9.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.9.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.9.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.9.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.9.2+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "31cbe04def6437cb576fe824b7afbd829607a5cd070590ceb2fb134581e26bec"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-6-560.28.03-1.x86_64.rpm"
            [["0.9.2+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "baac69caaa3543c777862ce71a9dd6c9cab9cefcfd24a40bfffa92697ee7faee"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-6-560.28.03-1.aarch64.rpm"

["0.10.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.0+0/CUDA_Driver.v0.10.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.0+0/CUDA_Driver.v0.10.0.x86_64-linux-gnu.tar.gz"]
registered = 2024-08-07T10:29:24.000Z

    [["0.10.0+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.0+0/CUDA_Driver.v0.10.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.0+0/CUDA_Driver.v0.10.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/0661e4526f0c50d5cfde8ef60621e31645aa85a0/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.5.8"

        ["0.10.0+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """    # global variables we will set\n    global libcuda = nothing\n\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    # find and select the system driver\n    libcuda_system = if Sys.iswindows()\n        Libdl.find_library(\"nvcuda\")\n    else\n        Libdl.find_library([\"libcuda.so.1\", \"libcuda.so\"])\n    end\n    if libcuda_system == \"\"\n        @debug \"No system driver found\"\n        return\n    end\n    @debug \"System driver found at $libcuda_system\"\n    libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if !@isdefined(libcuda_compat)\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n    @debug \"Forward-compatible driver found at $libcuda_compat\"\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # try to load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n        success(`$(Base.julia_cmd()) --compile=min -t1 --startup-file=no -e $script $driver $deps`)\n    end\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    if !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n\n    @debug \"Successfully loaded forwards-compatible CUDA driver.\"\n    libcuda = libcuda_compat\n\n    # load driver dependencies\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    # XXX: should we also load the driver here?\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.10.0"

            [["0.10.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.10.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.10.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.10.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.10.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "31cbe04def6437cb576fe824b7afbd829607a5cd070590ceb2fb134581e26bec"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-6-560.28.03-1.x86_64.rpm"
            [["0.10.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "baac69caaa3543c777862ce71a9dd6c9cab9cefcfd24a40bfffa92697ee7faee"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-6-560.28.03-1.aarch64.rpm"

["0.10.1+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.1+0/CUDA_Driver.v0.10.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.1+0/CUDA_Driver.v0.10.1.x86_64-linux-gnu.tar.gz"]
registered = 2024-09-17T19:35:42.000Z

    [["0.10.1+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.1+0/CUDA_Driver.v0.10.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.1+0/CUDA_Driver.v0.10.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/514036c40edeb847547a249fd0c6107416e0d9b9/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.0"

        ["0.10.1+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """    # global variables we will set\n    global libcuda = nothing\n\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    # find and select the system driver\n    libcuda_system = if Sys.iswindows()\n        Libdl.find_library(\"nvcuda\")\n    else\n        Libdl.find_library([\"libcuda.so.1\", \"libcuda.so\"])\n    end\n    if libcuda_system == \"\"\n        @debug \"No system driver found\"\n        return\n    end\n    @debug \"System driver found at $libcuda_system\"\n    libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if !@isdefined(libcuda_compat)\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n    @debug \"Forward-compatible driver found at $libcuda_compat\"\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # try to load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n        success(`$(Base.julia_cmd()) --compile=min -t1 --startup-file=no -e $script $driver $deps`)\n    end\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    if !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n\n    @debug \"Successfully loaded forwards-compatible CUDA driver.\"\n    libcuda = libcuda_compat\n\n    # load driver dependencies\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    # XXX: should we also load the driver here?\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.10.1"

            [["0.10.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.10.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.10.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.10.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.10.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "76895477da17dbf6efbbee327e0aa38ff230c69814127d59fed540fa9be1a162"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-6-560.35.03-1.x86_64.rpm"
            [["0.10.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "479530ff4f0c8759ccb140d7369f30056b7bba9520eb1b3822a138f882f87a38"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-6-560.35.03-1.aarch64.rpm"

["0.10.2+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.2+0/CUDA_Driver.v0.10.2.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.2+0/CUDA_Driver.v0.10.2.x86_64-linux-gnu.tar.gz"]
registered = 2024-09-18T13:17:00.000Z

    [["0.10.2+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.2+0/CUDA_Driver.v0.10.2.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.2+0/CUDA_Driver.v0.10.2.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/86d4db53dc4f579399387b13dd6ed6bfc6ca7f5c/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.0"

        ["0.10.2+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n        success(`$(Base.julia_cmd()) --compile=min -t1 --startup-file=no -e $script $driver $deps`)\n    end\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.10.2"

            [["0.10.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.10.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.10.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.10.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.10.2+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "76895477da17dbf6efbbee327e0aa38ff230c69814127d59fed540fa9be1a162"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-6-560.35.03-1.x86_64.rpm"
            [["0.10.2+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "479530ff4f0c8759ccb140d7369f30056b7bba9520eb1b3822a138f882f87a38"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-6-560.35.03-1.aarch64.rpm"

["0.10.3+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.3+0/CUDA_Driver.v0.10.3.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.3+0/CUDA_Driver.v0.10.3.x86_64-linux-gnu.tar.gz"]
registered = 2024-10-01T08:36:02.000Z

    [["0.10.3+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.3+0/CUDA_Driver.v0.10.3.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.3+0/CUDA_Driver.v0.10.3.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/edc7cf88161170bddf35499b3fb210a958adbecf/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.0"

        ["0.10.3+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        success(`$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec))) --compile=min -t1 --startup-file=no -e $script $driver $deps`)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.10.3"

            [["0.10.3+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.10.3+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.10.3+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.10.3+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.10.3+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "76895477da17dbf6efbbee327e0aa38ff230c69814127d59fed540fa9be1a162"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-6-560.35.03-1.x86_64.rpm"
            [["0.10.3+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "479530ff4f0c8759ccb140d7369f30056b7bba9520eb1b3822a138f882f87a38"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-6-560.35.03-1.aarch64.rpm"

["0.10.4+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.4+0/CUDA_Driver.v0.10.4.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.4+0/CUDA_Driver.v0.10.4.x86_64-linux-gnu.tar.gz"]
registered = 2024-11-22T18:55:00.000Z

    [["0.10.4+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.4+0/CUDA_Driver.v0.10.4.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.10.4+0/CUDA_Driver.v0.10.4.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/769c8c832548ab2425954de906f4f6b3a42176a3/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.0"

        ["0.10.4+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        success(`$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec))) --compile=min -t1 --startup-file=no -e $script $driver $deps`)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.10.4"

            [["0.10.4+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.10.4+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.10.4+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.10.4+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.10.4+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "0c5f6de2b07ca6f0526dab89200aac8bc2fbe218572175be278f40f6a6a0693c"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-6-560.35.05-1.el8.x86_64.rpm"
            [["0.10.4+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "eaa58bf35d500ea447ef6dfc3ee450e495f3508fb8cec118f39ca274c059205e"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-6-560.35.05-1.el8.aarch64.rpm"

["0.11.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.11.0+0/CUDA_Driver.v0.11.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.11.0+0/CUDA_Driver.v0.11.0.x86_64-linux-gnu.tar.gz"]
registered = 2024-12-09T18:24:25.000Z

    [["0.11.0+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.11.0+0/CUDA_Driver.v0.11.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.11.0+0/CUDA_Driver.v0.11.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/88bbdf7fc658047bbdcd49e0a357683eb9a3f78b/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.0"

        ["0.11.0+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        success(`$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec))) --compile=min -t1 --startup-file=no -e $script $driver $deps`)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.11.0"

            [["0.11.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.11.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.11.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.11.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.11.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "8df61c9a521494f4892082177fde778219be892a93e59a6fc1bceb4331f66225"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-7-565.57.01-1.el8.x86_64.rpm"
            [["0.11.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "68430934bc3de03cc00240b78da39099981fcedb81b48e69cbe4505dc36fffd7"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-7-565.57.01-1.el8.aarch64.rpm"

["0.12.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.0+0/CUDA_Driver.v0.12.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.0+0/CUDA_Driver.v0.12.0.x86_64-linux-gnu.tar.gz"]
registered = 2025-01-27T08:34:59.000Z

    [["0.12.0+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.0+0/CUDA_Driver.v0.12.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.0+0/CUDA_Driver.v0.12.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/77cacb314d44f2a739ad48e6bb55ad92a7afed44/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.3"

        ["0.12.0+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        success(`$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec))) --compile=min -t1 --startup-file=no -e $script $driver $deps`)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.12.0"

            [["0.12.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.12.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.12.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.12.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.12.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "012115a107352f3d0dd6b73206eaf8517477880a4be9329996612bcfd1621e2a"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-8-570.86.10-1.el8.x86_64.rpm"
            [["0.12.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "4fcc52fbf3b9323ddb3b26e799cd65bb8f73e122d65f27d78830f84117470fd6"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-8-570.86.10-1.el8.aarch64.rpm"

["0.12.1+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.1+0/CUDA_Driver.v0.12.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.1+0/CUDA_Driver.v0.12.1.x86_64-linux-gnu.tar.gz"]
registered = 2025-03-17T22:03:48.000Z

    [["0.12.1+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.1+0/CUDA_Driver.v0.12.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.1+0/CUDA_Driver.v0.12.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/568a97d6c6df8a224916249def3ea66e60defc79/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.4"

        ["0.12.1+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        success(`$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec))) --compile=min -t1 --startup-file=no -e $script $driver $deps`)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.12.1"

            [["0.12.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.12.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.12.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.12.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.12.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "1ddd595b6a7cd33414a8a4e1a05ab4d4c7d7b4b1d3aea87b347acd46da81399f"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-8-570.124.06-1.el8.x86_64.rpm"
            [["0.12.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "21869895263fb0f0ee9ed77a4d41e56349d50cfaaff3f8b8f213154a26d0f57c"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-8-570.124.06-1.el8.aarch64.rpm"

["0.12.1+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.1+1/CUDA_Driver.v0.12.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.1+1/CUDA_Driver.v0.12.1.x86_64-linux-gnu.tar.gz"]
registered = 2025-03-18T13:15:18.000Z

    [["0.12.1+1".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.1+1/CUDA_Driver.v0.12.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.12.1+1/CUDA_Driver.v0.12.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/bb312f5439f050d95efafd5c38697446e3cb6274/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.4"

        ["0.12.1+1".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 --compile=min -t1 --startup-file=no\n                 -e $script $driver $deps```\n        proc = withenv(\"JULIA_LOAD_PATH\"=> nothing, \"JULIA_DEPOT_PATH\"=> nothing) do\n            # make sure we use a fresh environment we can load Libdl in\n            run(ignorestatus(cmd))\n        end\n        success(proc)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.12.1"

            [["0.12.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.12.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.12.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.12.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.12.1+1".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "1ddd595b6a7cd33414a8a4e1a05ab4d4c7d7b4b1d3aea87b347acd46da81399f"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-8-570.124.06-1.el8.x86_64.rpm"
            [["0.12.1+1".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "21869895263fb0f0ee9ed77a4d41e56349d50cfaaff3f8b8f213154a26d0f57c"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-8-570.124.06-1.el8.aarch64.rpm"

["0.13.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.13.0+0/CUDA_Driver.v0.13.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.13.0+0/CUDA_Driver.v0.13.0.x86_64-linux-gnu.tar.gz"]
registered = 2025-05-07T08:05:26.000Z

    [["0.13.0+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.13.0+0/CUDA_Driver.v0.13.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.13.0+0/CUDA_Driver.v0.13.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/8eb83f9438f26224e462ae6cd740dba3cdf39dfd/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.4"

        ["0.13.0+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 --compile=min -t1 --startup-file=no\n                 -e $script $driver $deps```\n        proc = withenv(\"JULIA_LOAD_PATH\"=> nothing, \"JULIA_DEPOT_PATH\"=> nothing) do\n            # make sure we use a fresh environment we can load Libdl in\n            run(ignorestatus(cmd))\n        end\n        success(proc)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.13.0"

            [["0.13.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.13.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.13.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.13.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.13.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "683634bb4c718a038f2cdd509b75c837dc07b67364f8bb5e151ef76717085a75"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-9-575.51.03-1.el8.x86_64.rpm"
            [["0.13.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "9a9fc518042fbef8dedeb75c14806daef2a0712e052cd5664a0d3686d7cf3c97"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-9-575.51.03-1.el8.aarch64.rpm"

["0.13.1+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.13.1+0/CUDA_Driver.v0.13.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.13.1+0/CUDA_Driver.v0.13.1.x86_64-linux-gnu.tar.gz"]
registered = 2025-06-13T12:30:23.000Z

    [["0.13.1+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.13.1+0/CUDA_Driver.v0.13.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v0.13.1+0/CUDA_Driver.v0.13.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/58a94233f712bed2bd4114a23e36deb90cfbb084/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.4"

        ["0.13.1+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 --compile=min -t1 --startup-file=no\n                 -e $script $driver $deps```\n        proc = withenv(\"JULIA_LOAD_PATH\"=> nothing, \"JULIA_DEPOT_PATH\"=> nothing) do\n            # make sure we use a fresh environment we can load Libdl in\n            run(ignorestatus(cmd))\n        end\n        success(proc)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v0.13.1"

            [["0.13.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["0.13.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["0.13.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["0.13.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["0.13.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "89855e5ce2b80ed5fc48368000e1d540a126e8f30e04cd98f9d0e1cb00fc660b"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-9-575.57.08-1.el8.x86_64.rpm"
            [["0.13.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "cf5226deebc4847d5fc92d84abab6a842877d94463f754cafb8439d7512094e3"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-9-575.57.08-1.el8.aarch64.rpm"

["12.9.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v12.9.0+0/CUDA_Driver.v12.9.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v12.9.0+0/CUDA_Driver.v12.9.0.x86_64-linux-gnu.tar.gz"]
registered = 2025-06-25T11:23:12.000Z

    [["12.9.0+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v12.9.0+0/CUDA_Driver.v12.9.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v12.9.0+0/CUDA_Driver.v12.9.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/1d21e66f05e9e827fd4b217f60fbcb94e0ec74fa/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.4"

        ["12.9.0+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 --compile=min -t1 --startup-file=no\n                 -e $script $driver $deps```\n        proc = withenv(\"JULIA_LOAD_PATH\"=> nothing, \"JULIA_DEPOT_PATH\"=> nothing) do\n            # make sure we use a fresh environment we can load Libdl in\n            run(ignorestatus(cmd))\n        end\n        success(proc)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v12.9.0"

            [["12.9.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["12.9.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["12.9.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["12.9.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["12.9.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "89855e5ce2b80ed5fc48368000e1d540a126e8f30e04cd98f9d0e1cb00fc660b"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-12-9-575.57.08-1.el8.x86_64.rpm"
            [["12.9.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "cf5226deebc4847d5fc92d84abab6a842877d94463f754cafb8439d7512094e3"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-12-9-575.57.08-1.el8.aarch64.rpm"

["13.0.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+0/CUDA_Driver.v13.0.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+0/CUDA_Driver.v13.0.0.x86_64-linux-gnu.tar.gz"]
registered = 2025-08-05T11:52:48.000Z

    [["13.0.0+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+0/CUDA_Driver.v13.0.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+0/CUDA_Driver.v13.0.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/a1b120aa59e0b6ddcaca74e035b1f992efee339d/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.0.0+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n    can_use_compat = true\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        can_use_compat = false\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            can_use_compat = false\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        can_use_compat = false\n    end\n\n    # check if we can load the forward-compatible driver in a separate process\n    function try_driver(driver, deps)\n        script = raw\"\"\"\n            using Libdl\n            driver, deps... = ARGS\n\n            for dep in deps\n                Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n            end\n\n            library_handle = Libdl.dlopen(driver; throw_error=false)\n            library_handle === nothing && exit(-1)\n\n            function_handle = Libdl.dlsym(library_handle, \"cuInit\")\n            status = ccall(function_handle, Cint, (UInt32,), 0)\n            status == 0 || exit(-2)\n\n            exit(0)\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 --compile=min -t1 --startup-file=no\n                 -e $script $driver $deps```\n        proc = withenv(\"JULIA_LOAD_PATH\"=> nothing, \"JULIA_DEPOT_PATH\"=> nothing) do\n            # make sure we use a fresh environment we can load Libdl in\n            run(ignorestatus(cmd))\n        end\n        success(proc)\n    end\n\n    if can_use_compat && !try_driver(libcuda_compat, libcuda_deps)\n        @debug \"Failed to load forwards-compatible driver.\"\n        can_use_compat = false\n    end\n\n    # finally, load the appropriate driver\n    if can_use_compat\n        @debug \"Using forwards-compatible CUDA driver.\"\n        global libcuda = libcuda_compat\n\n        # load the driver and its dependencies; this should now always succeed\n        # as we've already verified that we can load it in a separate process.\n        for dep in libcuda_deps\n            Libdl.dlopen(dep; throw_error=true)\n        end\n        Libdl.dlopen(libcuda_compat; throw_error=true)\n    elseif Libdl.dlopen(libcuda_system; throw_error=false) !== nothing\n        @debug \"Using system CUDA driver.\"\n        global libcuda = libcuda_system\n    else\n        @debug \"Could not load system CUDA driver.\"\n        global libcuda = nothing\n    end\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v13.0.0"

            [["13.0.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.0.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.0.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.0.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["13.0.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "91690b17a70fe04a56aec463d18360b717c39ea8d23055b489919ae2c1255354"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-13-0-580.65.06-1.el8.x86_64.rpm"
            [["13.0.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "a44a8cb23eae9b97f5a76dec2a50a3b695eb9a38ce7fc0bd68896dda2ac54530"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-13-0-580.65.06-1.el8.aarch64.rpm"

["13.0.0+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+1/CUDA_Driver.v13.0.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+1/CUDA_Driver.v13.0.0.x86_64-linux-gnu.tar.gz"]
registered = 2025-09-03T14:46:07.000Z

    [["13.0.0+1".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+1/CUDA_Driver.v13.0.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+1/CUDA_Driver.v13.0.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/a1776c875b613199af7dd5cf9980a7a98cdf0a7a/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.0.0+1".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        script = raw\"\"\"\n            using Libdl\n\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR = 75\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR = 76\n\n            function main(driver, inspect_devices, deps...)\n                inspect_devices = parse(Bool, inspect_devices)\n\n                for dep in deps\n                    Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n                end\n\n                library_handle = Libdl.dlopen(driver; throw_error=false)\n                library_handle === nothing && return -1\n\n                cuInit = Libdl.dlsym(library_handle, \"cuInit\")\n                status = ccall(cuInit, Cint, (UInt32,), 0)\n                status == 0 || return -2\n\n                cuDriverGetVersion = Libdl.dlsym(library_handle, \"cuDriverGetVersion\")\n                version = Ref{Cint}()\n                status = ccall(cuDriverGetVersion, Cint, (Ptr{Cint},), version)\n                status == 0 || return -3\n                major, ver = divrem(version[], 1000)\n                minor, patch = divrem(ver, 10)\n                println(major, \".\", minor, \".\", patch)\n\n                if inspect_devices\n                    cuDeviceGetCount = Libdl.dlsym(library_handle, \"cuDeviceGetCount\")\n                    device_count = Ref{Cint}()\n                    status = ccall(cuDeviceGetCount, Cint, (Ptr{Cint},), device_count)\n                    status == 0 || return -4\n\n                    cuDeviceGet = Libdl.dlsym(library_handle, \"cuDeviceGet\")\n                    cuDeviceGetAttribute = Libdl.dlsym(library_handle, \"cuDeviceGetAttribute\")\n                    for i in 1:device_count[]\n                        device = Ref{Cint}()\n                        status = ccall(cuDeviceGet, Cint, (Ptr{Cint}, Cint), device, i-1)\n                        status == 0 || return -5\n\n                        major = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), major, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR, device[])\n                        status == 0 || return -6\n                        minor = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), minor, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR, device[])\n                        status == 0 || return -7\n                        println(major[], \".\", minor[])\n                    end\n                end\n\n                return 0\n            end\n\n            exit(main(ARGS...))\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 -O0 --compile=min -t1 --startup-file=no\n                 -e $script $driver $inspect_devices $deps```\n\n        # make sure we use a fresh environment we can load Libdl in\n        cmd = addenv(cmd, \"JULIA_LOAD_PATH\" => nothing, \"JULIA_DEPOT_PATH\" => nothing)\n\n        # run the command\n        out = Pipe()\n        proc = run(pipeline(cmd, stdout=out, stderr=stdout), wait=false)\n        close(out.in)\n        t = wait(proc)\n        success(proc) || return nothing\n\n        # parse the versions\n        version_strings = readlines(out)\n        driver_version = parse(VersionNumber, version_strings[1])\n        if inspect_devices\n            device_capabilities = map(str -> parse(VersionNumber, str), version_strings[2:end])\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n    compat_driver_task = Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n    system_driver_task = Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v13.0.0"

            [["13.0.0+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.0.0+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.0.0+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.0.0+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["13.0.0+1".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "91690b17a70fe04a56aec463d18360b717c39ea8d23055b489919ae2c1255354"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-13-0-580.65.06-1.el8.x86_64.rpm"
            [["13.0.0+1".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "a44a8cb23eae9b97f5a76dec2a50a3b695eb9a38ce7fc0bd68896dda2ac54530"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-13-0-580.65.06-1.el8.aarch64.rpm"

["13.0.0+2"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+2/CUDA_Driver.v13.0.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+2/CUDA_Driver.v13.0.0.x86_64-linux-gnu.tar.gz"]
registered = 2025-09-04T21:47:15.000Z

    [["13.0.0+2".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+2/CUDA_Driver.v13.0.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+2/CUDA_Driver.v13.0.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/ec99560b1fc84bbfbd17a604ca64c0ee4b1b9e09/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.0.0+2".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        script = raw\"\"\"\n            using Libdl\n\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR = 75\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR = 76\n\n            function main(driver, inspect_devices, deps...)\n                inspect_devices = parse(Bool, inspect_devices)\n\n                for dep in deps\n                    Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n                end\n\n                library_handle = Libdl.dlopen(driver; throw_error=false)\n                library_handle === nothing && return -1\n\n                cuInit = Libdl.dlsym(library_handle, \"cuInit\")\n                status = ccall(cuInit, Cint, (UInt32,), 0)\n                status == 0 || return -2\n\n                cuDriverGetVersion = Libdl.dlsym(library_handle, \"cuDriverGetVersion\")\n                version = Ref{Cint}()\n                status = ccall(cuDriverGetVersion, Cint, (Ptr{Cint},), version)\n                status == 0 || return -3\n                major, ver = divrem(version[], 1000)\n                minor, patch = divrem(ver, 10)\n                println(major, \".\", minor, \".\", patch)\n\n                if inspect_devices\n                    cuDeviceGetCount = Libdl.dlsym(library_handle, \"cuDeviceGetCount\")\n                    device_count = Ref{Cint}()\n                    status = ccall(cuDeviceGetCount, Cint, (Ptr{Cint},), device_count)\n                    status == 0 || return -4\n\n                    cuDeviceGet = Libdl.dlsym(library_handle, \"cuDeviceGet\")\n                    cuDeviceGetAttribute = Libdl.dlsym(library_handle, \"cuDeviceGetAttribute\")\n                    for i in 1:device_count[]\n                        device = Ref{Cint}()\n                        status = ccall(cuDeviceGet, Cint, (Ptr{Cint}, Cint), device, i-1)\n                        status == 0 || return -5\n\n                        major = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), major, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR, device[])\n                        status == 0 || return -6\n                        minor = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), minor, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR, device[])\n                        status == 0 || return -7\n                        println(major[], \".\", minor[])\n                    end\n                end\n\n                return 0\n            end\n\n            exit(main(ARGS...))\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 -O0 --compile=min -t1 --startup-file=no\n                 -e $script $driver $inspect_devices $deps```\n\n        # make sure we use a fresh environment we can load Libdl in\n        cmd = addenv(cmd, \"JULIA_LOAD_PATH\" => nothing, \"JULIA_DEPOT_PATH\" => nothing)\n\n        # run the command\n        out = Pipe()\n        proc = run(pipeline(cmd, stdin=devnull, stdout=out), wait=false)\n        close(out.in)\n        out_reader = Threads.@spawn String(readlines(out))\n        wait(proc)\n        success(proc) || return nothing\n\n        # parse the versions\n        version_strings = fetch(out_reader)\n        driver_version = parse(VersionNumber, version_strings[1])\n        if inspect_devices\n            device_capabilities = map(str -> parse(VersionNumber, str), version_strings[2:end])\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n    compat_driver_task = Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n    system_driver_task = Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v13.0.0"

            [["13.0.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.0.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.0.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.0.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["13.0.0+2".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "91690b17a70fe04a56aec463d18360b717c39ea8d23055b489919ae2c1255354"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-13-0-580.65.06-1.el8.x86_64.rpm"
            [["13.0.0+2".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "a44a8cb23eae9b97f5a76dec2a50a3b695eb9a38ce7fc0bd68896dda2ac54530"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-13-0-580.65.06-1.el8.aarch64.rpm"

["13.0.0+3"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+3/CUDA_Driver.v13.0.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+3/CUDA_Driver.v13.0.0.x86_64-linux-gnu.tar.gz"]
registered = 2025-09-08T10:09:13.000Z

    [["13.0.0+3".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+3/CUDA_Driver.v13.0.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.0+3/CUDA_Driver.v13.0.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/9f5c6e3fb6163271617365c1b219b5357ff8fc0b/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.0.0+3".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        script = raw\"\"\"\n            using Libdl\n\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR = 75\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR = 76\n\n            function main(driver, inspect_devices, deps...)\n                inspect_devices = parse(Bool, inspect_devices)\n\n                for dep in deps\n                    Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n                end\n\n                library_handle = Libdl.dlopen(driver; throw_error=false)\n                library_handle === nothing && return -1\n\n                cuInit = Libdl.dlsym(library_handle, \"cuInit\")\n                status = ccall(cuInit, Cint, (UInt32,), 0)\n                status == 0 || return -2\n\n                cuDriverGetVersion = Libdl.dlsym(library_handle, \"cuDriverGetVersion\")\n                version = Ref{Cint}()\n                status = ccall(cuDriverGetVersion, Cint, (Ptr{Cint},), version)\n                status == 0 || return -3\n                major, ver = divrem(version[], 1000)\n                minor, patch = divrem(ver, 10)\n                println(major, \".\", minor, \".\", patch)\n\n                if inspect_devices\n                    cuDeviceGetCount = Libdl.dlsym(library_handle, \"cuDeviceGetCount\")\n                    device_count = Ref{Cint}()\n                    status = ccall(cuDeviceGetCount, Cint, (Ptr{Cint},), device_count)\n                    status == 0 || return -4\n\n                    cuDeviceGet = Libdl.dlsym(library_handle, \"cuDeviceGet\")\n                    cuDeviceGetAttribute = Libdl.dlsym(library_handle, \"cuDeviceGetAttribute\")\n                    for i in 1:device_count[]\n                        device = Ref{Cint}()\n                        status = ccall(cuDeviceGet, Cint, (Ptr{Cint}, Cint), device, i-1)\n                        status == 0 || return -5\n\n                        major = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), major, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR, device[])\n                        status == 0 || return -6\n                        minor = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), minor, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR, device[])\n                        status == 0 || return -7\n                        println(major[], \".\", minor[])\n                    end\n                end\n\n                return 0\n            end\n\n            exit(main(ARGS...))\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 -O0 --compile=min -t1 --startup-file=no\n                 -e $script $driver $inspect_devices $deps```\n\n        # make sure we use a fresh environment we can load Libdl in\n        cmd = addenv(cmd, \"JULIA_LOAD_PATH\" => nothing, \"JULIA_DEPOT_PATH\" => nothing)\n\n        # run the command\n        out = Pipe()\n        proc = run(pipeline(cmd, stdin=devnull, stdout=out), wait=false)\n        close(out.in)\n        out_reader = Threads.@spawn String.(readlines(out))\n        wait(proc)\n        success(proc) || return nothing\n\n        # parse the versions\n        version_strings = fetch(out_reader)\n        driver_version = parse(VersionNumber, version_strings[1])\n        if inspect_devices\n            device_capabilities = map(str -> parse(VersionNumber, str), version_strings[2:end])\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n    compat_driver_task = Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n    system_driver_task = Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v13.0.0"

            [["13.0.0+3".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.0.0+3".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.0.0+3".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.0.0+3".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["13.0.0+3".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "91690b17a70fe04a56aec463d18360b717c39ea8d23055b489919ae2c1255354"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-13-0-580.65.06-1.el8.x86_64.rpm"
            [["13.0.0+3".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "a44a8cb23eae9b97f5a76dec2a50a3b695eb9a38ce7fc0bd68896dda2ac54530"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-13-0-580.65.06-1.el8.aarch64.rpm"

["13.0.1+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.1+0/CUDA_Driver.v13.0.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.1+0/CUDA_Driver.v13.0.1.x86_64-linux-gnu.tar.gz"]
registered = 2025-09-09T13:31:21.000Z

    [["13.0.1+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.1+0/CUDA_Driver.v13.0.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.1+0/CUDA_Driver.v13.0.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/95bd7de3aec7e15eba7cb0b0c9030953a8e1f78b/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.0.1+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        script = raw\"\"\"\n            using Libdl\n\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR = 75\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR = 76\n\n            function main(driver, inspect_devices, deps...)\n                inspect_devices = parse(Bool, inspect_devices)\n\n                for dep in deps\n                    Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n                end\n\n                library_handle = Libdl.dlopen(driver; throw_error=false)\n                library_handle === nothing && return -1\n\n                cuInit = Libdl.dlsym(library_handle, \"cuInit\")\n                status = ccall(cuInit, Cint, (UInt32,), 0)\n                status == 0 || return -2\n\n                cuDriverGetVersion = Libdl.dlsym(library_handle, \"cuDriverGetVersion\")\n                version = Ref{Cint}()\n                status = ccall(cuDriverGetVersion, Cint, (Ptr{Cint},), version)\n                status == 0 || return -3\n                major, ver = divrem(version[], 1000)\n                minor, patch = divrem(ver, 10)\n                println(major, \".\", minor, \".\", patch)\n\n                if inspect_devices\n                    cuDeviceGetCount = Libdl.dlsym(library_handle, \"cuDeviceGetCount\")\n                    device_count = Ref{Cint}()\n                    status = ccall(cuDeviceGetCount, Cint, (Ptr{Cint},), device_count)\n                    status == 0 || return -4\n\n                    cuDeviceGet = Libdl.dlsym(library_handle, \"cuDeviceGet\")\n                    cuDeviceGetAttribute = Libdl.dlsym(library_handle, \"cuDeviceGetAttribute\")\n                    for i in 1:device_count[]\n                        device = Ref{Cint}()\n                        status = ccall(cuDeviceGet, Cint, (Ptr{Cint}, Cint), device, i-1)\n                        status == 0 || return -5\n\n                        major = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), major, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR, device[])\n                        status == 0 || return -6\n                        minor = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), minor, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR, device[])\n                        status == 0 || return -7\n                        println(major[], \".\", minor[])\n                    end\n                end\n\n                return 0\n            end\n\n            exit(main(ARGS...))\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 -O0 --compile=min -t1 --startup-file=no\n                 -e $script $driver $inspect_devices $deps```\n\n        # make sure we use a fresh environment we can load Libdl in\n        cmd = addenv(cmd, \"JULIA_LOAD_PATH\" => nothing, \"JULIA_DEPOT_PATH\" => nothing)\n\n        # run the command\n        out = Pipe()\n        proc = run(pipeline(cmd, stdin=devnull, stdout=out), wait=false)\n        close(out.in)\n        out_reader = Threads.@spawn String.(readlines(out))\n        wait(proc)\n        success(proc) || return nothing\n\n        # parse the versions\n        version_strings = fetch(out_reader)\n        driver_version = parse(VersionNumber, version_strings[1])\n        if inspect_devices\n            device_capabilities = map(str -> parse(VersionNumber, str), version_strings[2:end])\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n    compat_driver_task = Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n    system_driver_task = Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v13.0.1"

            [["13.0.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.0.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.0.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.0.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["13.0.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "786b3105973ff8e3d7a1be6b0f9b512b1e1db9a8af30ae4681f8290fb254ddc2"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-13-0-580.82.07-1.el8.x86_64.rpm"
            [["13.0.1+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "1d8b3165d52d3ee9e7b94700ebef173ceb359706065edfc392e7f95a75b6fef4"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-13-0-580.82.07-1.el8.aarch64.rpm"

["13.0.1+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.1+1/CUDA_Driver.v13.0.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.1+1/CUDA_Driver.v13.0.1.x86_64-linux-gnu.tar.gz"]
registered = 2025-10-15T08:28:57.000Z

    [["13.0.1+1".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.1+1/CUDA_Driver.v13.0.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.1+1/CUDA_Driver.v13.0.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/98e42ddf7902aaa84fdf6ca395bd1107d03020f0/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.0.1+1".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        script = raw\"\"\"\n            using Libdl\n\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR = 75\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR = 76\n\n            function main(driver, inspect_devices, deps...)\n                inspect_devices = parse(Bool, inspect_devices)\n\n                for dep in deps\n                    Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n                end\n\n                library_handle = Libdl.dlopen(driver; throw_error=false)\n                library_handle === nothing && return -1\n\n                cuInit = Libdl.dlsym(library_handle, \"cuInit\")\n                status = ccall(cuInit, Cint, (UInt32,), 0)\n                status == 0 || return -2\n\n                cuDriverGetVersion = Libdl.dlsym(library_handle, \"cuDriverGetVersion\")\n                version = Ref{Cint}()\n                status = ccall(cuDriverGetVersion, Cint, (Ptr{Cint},), version)\n                status == 0 || return -3\n                major, ver = divrem(version[], 1000)\n                minor, patch = divrem(ver, 10)\n                println(major, \".\", minor, \".\", patch)\n\n                if inspect_devices\n                    cuDeviceGetCount = Libdl.dlsym(library_handle, \"cuDeviceGetCount\")\n                    device_count = Ref{Cint}()\n                    status = ccall(cuDeviceGetCount, Cint, (Ptr{Cint},), device_count)\n                    status == 0 || return -4\n\n                    cuDeviceGet = Libdl.dlsym(library_handle, \"cuDeviceGet\")\n                    cuDeviceGetAttribute = Libdl.dlsym(library_handle, \"cuDeviceGetAttribute\")\n                    for i in 1:device_count[]\n                        device = Ref{Cint}()\n                        status = ccall(cuDeviceGet, Cint, (Ptr{Cint}, Cint), device, i-1)\n                        status == 0 || return -5\n\n                        major = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), major, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR, device[])\n                        status == 0 || return -6\n                        minor = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), minor, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR, device[])\n                        status == 0 || return -7\n                        println(major[], \".\", minor[])\n                    end\n                end\n\n                return 0\n            end\n\n            exit(main(ARGS...))\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 -O0 --compile=min -t1 --startup-file=no\n                 -e $script $driver $inspect_devices $deps```\n\n        # make sure we use a fresh environment we can load Libdl in\n        cmd = addenv(cmd, \"JULIA_LOAD_PATH\" => nothing, \"JULIA_DEPOT_PATH\" => nothing)\n\n        # run the command\n        out = Pipe()\n        proc = run(pipeline(cmd, stdin=devnull, stdout=out), wait=false)\n        close(out.in)\n        out_reader = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool String.(readlines(out))\n        else\n            Threads.@spawn String.(readlines(out))\n        end\n        wait(proc)\n        success(proc) || return nothing\n\n        # parse the versions\n        version_strings = fetch(out_reader)\n        driver_version = parse(VersionNumber, version_strings[1])\n        if inspect_devices\n            device_capabilities = map(str -> parse(VersionNumber, str), version_strings[2:end])\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n        compat_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_compat, libcuda_deps)\n        else\n            Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n        end\n        system_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_system; inspect_devices=true)\n        else\n            Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n        end\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v13.0.1"

            [["13.0.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.0.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.0.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.0.1+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["13.0.1+1".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "786b3105973ff8e3d7a1be6b0f9b512b1e1db9a8af30ae4681f8290fb254ddc2"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-13-0-580.82.07-1.el8.x86_64.rpm"
            [["13.0.1+1".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "1d8b3165d52d3ee9e7b94700ebef173ceb359706065edfc392e7f95a75b6fef4"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-13-0-580.82.07-1.el8.aarch64.rpm"

["13.0.2+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.2+0/CUDA_Driver.v13.0.2.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.2+0/CUDA_Driver.v13.0.2.x86_64-linux-gnu.tar.gz"]
registered = 2025-10-16T23:35:19.000Z

    [["13.0.2+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.2+0/CUDA_Driver.v13.0.2.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.0.2+0/CUDA_Driver.v13.0.2.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/a9adbb8e61cf4e590ec1560a4d8649f5771c1f61/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.0.2+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        script = raw\"\"\"\n            using Libdl\n\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR = 75\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR = 76\n\n            function main(driver, inspect_devices, deps...)\n                inspect_devices = parse(Bool, inspect_devices)\n\n                for dep in deps\n                    Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n                end\n\n                library_handle = Libdl.dlopen(driver; throw_error=false)\n                library_handle === nothing && return -1\n\n                cuInit = Libdl.dlsym(library_handle, \"cuInit\")\n                status = ccall(cuInit, Cint, (UInt32,), 0)\n                status == 0 || return -2\n\n                cuDriverGetVersion = Libdl.dlsym(library_handle, \"cuDriverGetVersion\")\n                version = Ref{Cint}()\n                status = ccall(cuDriverGetVersion, Cint, (Ptr{Cint},), version)\n                status == 0 || return -3\n                major, ver = divrem(version[], 1000)\n                minor, patch = divrem(ver, 10)\n                println(major, \".\", minor, \".\", patch)\n\n                if inspect_devices\n                    cuDeviceGetCount = Libdl.dlsym(library_handle, \"cuDeviceGetCount\")\n                    device_count = Ref{Cint}()\n                    status = ccall(cuDeviceGetCount, Cint, (Ptr{Cint},), device_count)\n                    status == 0 || return -4\n\n                    cuDeviceGet = Libdl.dlsym(library_handle, \"cuDeviceGet\")\n                    cuDeviceGetAttribute = Libdl.dlsym(library_handle, \"cuDeviceGetAttribute\")\n                    for i in 1:device_count[]\n                        device = Ref{Cint}()\n                        status = ccall(cuDeviceGet, Cint, (Ptr{Cint}, Cint), device, i-1)\n                        status == 0 || return -5\n\n                        major = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), major, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR, device[])\n                        status == 0 || return -6\n                        minor = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), minor, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR, device[])\n                        status == 0 || return -7\n                        println(major[], \".\", minor[])\n                    end\n                end\n\n                return 0\n            end\n\n            exit(main(ARGS...))\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 -O0 --compile=min -t1 --startup-file=no\n                 -e $script $driver $inspect_devices $deps```\n\n        # make sure we use a fresh environment we can load Libdl in\n        cmd = addenv(cmd, \"JULIA_LOAD_PATH\" => nothing, \"JULIA_DEPOT_PATH\" => nothing)\n\n        # run the command\n        out = Pipe()\n        proc = run(pipeline(cmd, stdin=devnull, stdout=out), wait=false)\n        close(out.in)\n        out_reader = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool String.(readlines(out))\n        else\n            Threads.@spawn String.(readlines(out))\n        end\n        wait(proc)\n        success(proc) || return nothing\n\n        # parse the versions\n        version_strings = fetch(out_reader)\n        driver_version = parse(VersionNumber, version_strings[1])\n        if inspect_devices\n            device_capabilities = map(str -> parse(VersionNumber, str), version_strings[2:end])\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n        compat_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_compat, libcuda_deps)\n        else\n            Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n        end\n        system_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_system; inspect_devices=true)\n        else\n            Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n        end\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v13.0.2"

            [["13.0.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.0.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.0.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.0.2+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["13.0.2+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "81cfb0e12c2ccf94e992e75cd15d830080b7b47a53b25fe3d14896c9c2478c76"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-13-0-580.95.05-1.el8.x86_64.rpm"
            [["13.0.2+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "7e393079b34023f8fc7a648d9cf943f0d4ea34a9261050ba3c8c620bafb28eb5"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-13-0-580.95.05-1.el8.aarch64.rpm"

["13.1.0+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+0/CUDA_Driver.v13.1.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+0/CUDA_Driver.v13.1.0.x86_64-linux-gnu.tar.gz"]
registered = 2025-12-16T13:36:06.000Z

    [["13.1.0+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+0/CUDA_Driver.v13.1.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+0/CUDA_Driver.v13.1.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/9853ca382b4f1f7902260e663cb37f5c3a6c8e8d/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.1.0+0".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        script = raw\"\"\"\n            using Libdl\n\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR = 75\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR = 76\n\n            function main(driver, inspect_devices, deps...)\n                inspect_devices = parse(Bool, inspect_devices)\n\n                for dep in deps\n                    Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n                end\n\n                library_handle = Libdl.dlopen(driver; throw_error=false)\n                library_handle === nothing && return -1\n\n                cuInit = Libdl.dlsym(library_handle, \"cuInit\")\n                status = ccall(cuInit, Cint, (UInt32,), 0)\n                status == 0 || return -2\n\n                cuDriverGetVersion = Libdl.dlsym(library_handle, \"cuDriverGetVersion\")\n                version = Ref{Cint}()\n                status = ccall(cuDriverGetVersion, Cint, (Ptr{Cint},), version)\n                status == 0 || return -3\n                major, ver = divrem(version[], 1000)\n                minor, patch = divrem(ver, 10)\n                println(major, \".\", minor, \".\", patch)\n\n                if inspect_devices\n                    cuDeviceGetCount = Libdl.dlsym(library_handle, \"cuDeviceGetCount\")\n                    device_count = Ref{Cint}()\n                    status = ccall(cuDeviceGetCount, Cint, (Ptr{Cint},), device_count)\n                    status == 0 || return -4\n\n                    cuDeviceGet = Libdl.dlsym(library_handle, \"cuDeviceGet\")\n                    cuDeviceGetAttribute = Libdl.dlsym(library_handle, \"cuDeviceGetAttribute\")\n                    for i in 1:device_count[]\n                        device = Ref{Cint}()\n                        status = ccall(cuDeviceGet, Cint, (Ptr{Cint}, Cint), device, i-1)\n                        status == 0 || return -5\n\n                        major = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), major, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR, device[])\n                        status == 0 || return -6\n                        minor = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), minor, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR, device[])\n                        status == 0 || return -7\n                        println(major[], \".\", minor[])\n                    end\n                end\n\n                return 0\n            end\n\n            exit(main(ARGS...))\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 -O0 --compile=min -t1 --startup-file=no\n                 -e $script $driver $inspect_devices $deps```\n\n        # make sure we use a fresh environment we can load Libdl in\n        cmd = addenv(cmd, \"JULIA_LOAD_PATH\" => nothing, \"JULIA_DEPOT_PATH\" => nothing)\n\n        # run the command\n        out = Pipe()\n        proc = run(pipeline(cmd, stdin=devnull, stdout=out), wait=false)\n        close(out.in)\n        out_reader = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool String.(readlines(out))\n        else\n            Threads.@spawn String.(readlines(out))\n        end\n        wait(proc)\n        success(proc) || return nothing\n\n        # parse the versions\n        version_strings = fetch(out_reader)\n        driver_version = parse(VersionNumber, version_strings[1])\n        if inspect_devices\n            device_capabilities = map(str -> parse(VersionNumber, str), version_strings[2:end])\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n        compat_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_compat, libcuda_deps)\n        else\n            Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n        end\n        system_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_system; inspect_devices=true)\n        else\n            Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n        end\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    apk update\n    apk add rpm2cpio\n    rpm2cpio compat.rpm | cpio -idmv\n\n    mkdir -p ${libdir}\n\n    mv usr/local/cuda-*/compat/* ${libdir}\n"""
        version = "v13.1.0"

            [["13.1.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.1.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.1.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.1.0+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["13.1.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "a1c9f934c6c5b4a566c907f9cc55e8505b17b00b35163fdb5342c4811cd33798"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-compat-13-1-590.44.01-1.el8.x86_64.rpm"
            [["13.1.0+0".artifact_metadata.metadata.sources]]
            filename = "compat.rpm"
            hash = "e7233912d7a2f4529673a70702f3dd5a1350228db51e31c0f160468fde2663bf"
            type = "file"
            url = "https://developer.download.nvidia.com/compute/cuda/repos/rhel8/sbsa/cuda-compat-13-1-590.44.01-1.el8.aarch64.rpm"

["13.1.0+1"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+1/CUDA_Driver.v13.1.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+1/CUDA_Driver.v13.1.0.x86_64-linux-gnu.tar.gz"]
registered = 2026-01-09T14:44:40.000Z

    [["13.1.0+1".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+1/CUDA_Driver.v13.1.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+1/CUDA_Driver.v13.1.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/6f452731dc6b72e9514dbf758460c52123599f70/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.1.0+1".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        script = raw\"\"\"\n            using Libdl\n\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR = 75\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR = 76\n\n            function main(driver, inspect_devices, deps...)\n                inspect_devices = parse(Bool, inspect_devices)\n\n                for dep in deps\n                    Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n                end\n\n                library_handle = Libdl.dlopen(driver; throw_error=false)\n                library_handle === nothing && return -1\n\n                cuInit = Libdl.dlsym(library_handle, \"cuInit\")\n                status = ccall(cuInit, Cint, (UInt32,), 0)\n                status == 0 || return -2\n\n                cuDriverGetVersion = Libdl.dlsym(library_handle, \"cuDriverGetVersion\")\n                version = Ref{Cint}()\n                status = ccall(cuDriverGetVersion, Cint, (Ptr{Cint},), version)\n                status == 0 || return -3\n                major, ver = divrem(version[], 1000)\n                minor, patch = divrem(ver, 10)\n                println(major, \".\", minor, \".\", patch)\n\n                if inspect_devices\n                    cuDeviceGetCount = Libdl.dlsym(library_handle, \"cuDeviceGetCount\")\n                    device_count = Ref{Cint}()\n                    status = ccall(cuDeviceGetCount, Cint, (Ptr{Cint},), device_count)\n                    status == 0 || return -4\n\n                    cuDeviceGet = Libdl.dlsym(library_handle, \"cuDeviceGet\")\n                    cuDeviceGetAttribute = Libdl.dlsym(library_handle, \"cuDeviceGetAttribute\")\n                    for i in 1:device_count[]\n                        device = Ref{Cint}()\n                        status = ccall(cuDeviceGet, Cint, (Ptr{Cint}, Cint), device, i-1)\n                        status == 0 || return -5\n\n                        major = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), major, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR, device[])\n                        status == 0 || return -6\n                        minor = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), minor, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR, device[])\n                        status == 0 || return -7\n                        println(major[], \".\", minor[])\n                    end\n                end\n\n                return 0\n            end\n\n            exit(main(ARGS...))\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 -O0 --compile=min -t1 --startup-file=no\n                 -e $script $driver $inspect_devices $deps```\n\n        # make sure we use a fresh environment we can load Libdl in\n        cmd = addenv(cmd, \"JULIA_LOAD_PATH\" => nothing, \"JULIA_DEPOT_PATH\" => nothing)\n\n        # run the command\n        out = Pipe()\n        proc = run(pipeline(cmd, stdin=devnull, stdout=out), wait=false)\n        close(out.in)\n        out_reader = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool String.(readlines(out))\n        else\n            Threads.@spawn String.(readlines(out))\n        end\n        wait(proc)\n        success(proc) || return nothing\n\n        # parse the versions\n        version_strings = fetch(out_reader)\n        driver_version = parse(VersionNumber, version_strings[1])\n        if inspect_devices\n            device_capabilities = map(str -> parse(VersionNumber, str), version_strings[2:end])\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n        compat_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_compat, libcuda_deps)\n        else\n            Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n        end\n        system_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_system; inspect_devices=true)\n        else\n            Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n        end\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    mkdir -p ${libdir}\n\n    cd ${WORKSPACE}/srcdir/cuda_compat*\n\n    install_license LICENSE\n\n    mv compat/* ${libdir}\n"""
        version = "v13.1.0"

            [["13.1.0+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.1.0+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.1.0+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.1.0+1".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"

            [["13.1.0+1".artifact_metadata.metadata.sources]]
            hash = "0e8c1ec14c2abbce76943cb32907b39237a41bf267ec6665f04d48b240ae539f"
            type = "archive"
            unpack_target = ""
            url = "https://developer.download.nvidia.com/compute/nvidia-driver/redist/cuda_compat/linux-x86_64/cuda_compat-linux-x86_64-590.48.01_cuda13.1-archive.tar.xz"
            [["13.1.0+1".artifact_metadata.metadata.sources]]
            hash = "f3e02edc1a1080f44ec69478011e73218c7e20eaf52422963fd084ad0310bb64"
            type = "archive"
            unpack_target = ""
            url = "https://developer.download.nvidia.com/compute/nvidia-driver/redist/cuda_compat/linux-sbsa/cuda_compat-linux-sbsa-590.48.01_cuda13.1-archive.tar.xz"

["13.1.0+2"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+2/CUDA_Driver.v13.1.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+2/CUDA_Driver.v13.1.0.x86_64-linux-gnu.tar.gz"]
registered = 2026-01-12T09:34:49.000Z

    [["13.1.0+2".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+2/CUDA_Driver.v13.1.0.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.0+2/CUDA_Driver.v13.1.0.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/a6976a08c4e25d9e2d10b198016d6ebbb6ad948a/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.5"

        ["13.1.0+2".artifact_metadata.metadata]
        augment_platform_block = ""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler, libnvidia_gpucomp, libnvidia_tileiras]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        script = raw\"\"\"\n            using Libdl\n\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR = 75\n            const DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR = 76\n\n            function main(driver, inspect_devices, deps...)\n                inspect_devices = parse(Bool, inspect_devices)\n\n                for dep in deps\n                    Libdl.dlopen(dep; throw_error=false) === nothing && exit(-1)\n                end\n\n                library_handle = Libdl.dlopen(driver; throw_error=false)\n                library_handle === nothing && return -1\n\n                cuInit = Libdl.dlsym(library_handle, \"cuInit\")\n                status = ccall(cuInit, Cint, (UInt32,), 0)\n                status == 0 || return -2\n\n                cuDriverGetVersion = Libdl.dlsym(library_handle, \"cuDriverGetVersion\")\n                version = Ref{Cint}()\n                status = ccall(cuDriverGetVersion, Cint, (Ptr{Cint},), version)\n                status == 0 || return -3\n                major, ver = divrem(version[], 1000)\n                minor, patch = divrem(ver, 10)\n                println(major, \".\", minor, \".\", patch)\n\n                if inspect_devices\n                    cuDeviceGetCount = Libdl.dlsym(library_handle, \"cuDeviceGetCount\")\n                    device_count = Ref{Cint}()\n                    status = ccall(cuDeviceGetCount, Cint, (Ptr{Cint},), device_count)\n                    status == 0 || return -4\n\n                    cuDeviceGet = Libdl.dlsym(library_handle, \"cuDeviceGet\")\n                    cuDeviceGetAttribute = Libdl.dlsym(library_handle, \"cuDeviceGetAttribute\")\n                    for i in 1:device_count[]\n                        device = Ref{Cint}()\n                        status = ccall(cuDeviceGet, Cint, (Ptr{Cint}, Cint), device, i-1)\n                        status == 0 || return -5\n\n                        major = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), major, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MAJOR, device[])\n                        status == 0 || return -6\n                        minor = Ref{Cint}()\n                        status = ccall(cuDeviceGetAttribute, Cint, (Ptr{Cint}, UInt32, Cint), minor, DEVICE_ATTRIBUTE_COMPUTE_CAPABILITY_MINOR, device[])\n                        status == 0 || return -7\n                        println(major[], \".\", minor[])\n                    end\n                end\n\n                return 0\n            end\n\n            exit(main(ARGS...))\n        \"\"\"\n\n        # make sure we don't include any system image flags here since this will cause an infinite loop of __init__()\n        cmd = ```$(Cmd(filter(!startswith(r\"-J|--sysimage\"), Base.julia_cmd().exec)))\n                 -O0 --compile=min -t1 --startup-file=no\n                 -e $script $driver $inspect_devices $deps```\n\n        # make sure we use a fresh environment we can load Libdl in\n        cmd = addenv(cmd, \"JULIA_LOAD_PATH\" => nothing, \"JULIA_DEPOT_PATH\" => nothing)\n\n        # run the command\n        out = Pipe()\n        proc = run(pipeline(cmd, stdin=devnull, stdout=out), wait=false)\n        close(out.in)\n        out_reader = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool String.(readlines(out))\n        else\n            Threads.@spawn String.(readlines(out))\n        end\n        wait(proc)\n        success(proc) || return nothing\n\n        # parse the versions\n        version_strings = fetch(out_reader)\n        driver_version = parse(VersionNumber, version_strings[1])\n        if inspect_devices\n            device_capabilities = map(str -> parse(VersionNumber, str), version_strings[2:end])\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n        compat_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_compat, libcuda_deps)\n        else\n            Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n        end\n        system_driver_task = @static if VERSION >= v\"1.12-\"\n            # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n            Threads.@spawn :samepool inspect_driver(libcuda_system; inspect_devices=true)\n        else\n            Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n        end\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.0"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    mkdir -p ${libdir}\n\n    cd ${WORKSPACE}/srcdir/cuda_compat*\n\n    install_license LICENSE\n\n    mv compat/* ${libdir}\n"""
        version = "v13.1.0"

            [["13.1.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.1.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.1.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-gpucomp"]
            type = "lib"
            variable_name = "libnvidia_gpucomp"
            [["13.1.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.1.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"
            [["13.1.0+2".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-tileiras"]
            type = "lib"
            variable_name = "libnvidia_tileiras"

            [["13.1.0+2".artifact_metadata.metadata.sources]]
            hash = "0e8c1ec14c2abbce76943cb32907b39237a41bf267ec6665f04d48b240ae539f"
            type = "archive"
            unpack_target = ""
            url = "https://developer.download.nvidia.com/compute/nvidia-driver/redist/cuda_compat/linux-x86_64/cuda_compat-linux-x86_64-590.48.01_cuda13.1-archive.tar.xz"
            [["13.1.0+2".artifact_metadata.metadata.sources]]
            hash = "f3e02edc1a1080f44ec69478011e73218c7e20eaf52422963fd084ad0310bb64"
            type = "archive"
            unpack_target = ""
            url = "https://developer.download.nvidia.com/compute/nvidia-driver/redist/cuda_compat/linux-sbsa/cuda_compat-linux-sbsa-590.48.01_cuda13.1-archive.tar.xz"

["13.1.1+0"]
artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.1+0/CUDA_Driver.v13.1.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.1+0/CUDA_Driver.v13.1.1.x86_64-linux-gnu.tar.gz"]
registered = 2026-03-05T15:17:06.000Z

    [["13.1.1+0".artifact_metadata]]
    artifact_urls = ["https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.1+0/CUDA_Driver.v13.1.1.aarch64-linux-gnu.tar.gz", "https://github.com/JuliaBinaryWrappers/CUDA_Driver_jll.jl/releases/download/CUDA_Driver-v13.1.1+0/CUDA_Driver.v13.1.1.x86_64-linux-gnu.tar.gz"]
    buildscript = "https://github.com/JuliaPackaging/Yggdrasil/tree/0fda3618402b27afbd37f370022c9224d59b4bd8/C/CUDA/CUDA_Driver/build_tarballs.jl"
    metadata_source = "retrospective (by README link)"
    metadata_type = "BinaryBuilder --meta-json"
    metadata_version = "0.6.6"

        ["13.1.1+0".artifact_metadata.metadata]
        augment_platform_block = """# Precompile the process-spawning and version-parsing hot path\nprecompile(Tuple{typeof(Base.cmd_gen), Tuple{Tuple{Base.Cmd}, Tuple{String}, Tuple{Bool}, Tuple{Array{String, 1}}}})\nprecompile(Tuple{typeof(Base.arg_gen), Bool})\nprecompile(Tuple{typeof(Base.read), Base.Cmd, Type{String}})\nprecompile(Tuple{typeof(Base.readlines), Base.IOBuffer})\nprecompile(Tuple{typeof(Base.push!), Array{Base.VersionNumber, 1}, Base.VersionNumber})\nprecompile(Tuple{typeof(Base.Iterators.enumerate), Array{Base.VersionNumber, 1}})\nprecompile(Tuple{typeof(Base.iterate), Base.Iterators.Enumerate{Array{Base.VersionNumber, 1}}})\nprecompile(Tuple{typeof(Base.iterate), Base.Iterators.Enumerate{Array{Base.VersionNumber, 1}}, Tuple{Int64, Int64}})\nprecompile(Tuple{typeof(Base.indexed_iterate), Tuple{Int64, Base.VersionNumber}, Int64})\nprecompile(Tuple{typeof(Base.indexed_iterate), Tuple{Int64, Base.VersionNumber}, Int64, Int64})\n\naugment_platform! = identity\n"""
        dependencies = []
        init_block = """\n    # manual use of preferences, as we can't depend on additional packages in JLLs.\n    CUDA_Driver_jll_uuid = Base.UUID(\"4ee394cb-3365-5eb0-8335-949819d2adfc\")\n    preferences = Base.get_preferences(CUDA_Driver_jll_uuid)\n    function parse_preference(val)\n        if isa(val, Bool)\n            val\n        elseif isa(val, String)\n            parsed = tryparse(Bool, val)\n            if parsed === nothing\n                @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n                missing\n            else\n                parsed\n            end\n        else\n            @error \"CUDA compat preference is not valid; expected a boolean, but got '$val'\"\n            missing\n        end\n    end\n    compat_preference = if haskey(preferences, \"compat\")\n        parse_preference(preferences[\"compat\"])\n    elseif haskey(ENV, \"JULIA_CUDA_USE_COMPAT\")\n        parse_preference(ENV[\"JULIA_CUDA_USE_COMPAT\"])\n    else\n        missing\n    end\n\n    libcuda_deps = [libcuda_debugger, libnvidia_nvvm, libnvidia_ptxjitcompiler, libnvidia_gpucomp, libnvidia_tileiras]\n    libcuda_system = Sys.iswindows() ? \"nvcuda\" : \"libcuda.so.1\"\n\n    # if anything goes wrong, we'll use the system driver\n    global libcuda = libcuda_system\n\n    # check if we even have an artifact\n    if @isdefined(libcuda_compat)\n        @debug \"Forward-compatible driver found at $libcuda_compat\"\n    else\n        @debug \"No forward-compatible driver available for your platform.\"\n        return\n    end\n\n    # check the user preference\n    if compat_preference !== missing\n        if !compat_preference\n            @debug \"User disallows using forward-compatible driver.\"\n            return\n        end\n    end\n\n    # check if the system driver is already loaded. in that case, we have to use it because\n    # the code that loaded it in the first place might have made assumptions based on it.\n    if Libdl.dlopen(libcuda_system, Libdl.RTLD_NOLOAD; throw_error=false) !== nothing\n        @debug \"System CUDA driver already loaded, continuing using it.\"\n        return\n    end\n\n    # helper function to load a driver, query its version, and optionally query device\n    # capabilities. needs to happen in a separate process because dlclose is unreliable.\n    function inspect_driver(driver, deps=String[]; inspect_devices=false)\n        cmd = `$(cuda_inspect_driver()) $driver $inspect_devices $deps`\n\n        # run the command\n        output = try\n            read(cmd, String)\n        catch _\n            return nothing\n        end\n\n        # parse the versions\n        lines = readlines(IOBuffer(output))\n        isempty(lines) && return nothing\n        driver_version = parse(VersionNumber, lines[1])\n        if inspect_devices\n            device_capabilities = VersionNumber[]\n            for i in 2:length(lines)\n                push!(device_capabilities, parse(VersionNumber, lines[i]))\n            end\n            return driver_version, device_capabilities\n        else\n            return driver_version\n        end\n    end\n\n    # fetch driver details\n    compat_driver_task = @static if VERSION >= v\"1.12-\"\n        # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n        Threads.@spawn :samepool inspect_driver(libcuda_compat, libcuda_deps)\n    else\n        Threads.@spawn inspect_driver(libcuda_compat, libcuda_deps)\n    end\n    system_driver_task = @static if VERSION >= v\"1.12-\"\n        # XXX: avoid concurrent compilation (JuliaLang/julia#59834)\n        Threads.@spawn :samepool inspect_driver(libcuda_system; inspect_devices=true)\n    else\n        Threads.@spawn inspect_driver(libcuda_system; inspect_devices=true)\n    end\n    compat_driver_details = fetch(compat_driver_task)\n    if compat_driver_details === nothing\n        @debug \"Failed to load forwards-compatible driver.\"\n        return\n    end\n    compat_driver_version = compat_driver_details::VersionNumber\n    @debug \"Forwards compatible driver version: $compat_driver_version\"\n    system_driver_details = fetch(system_driver_task)\n    if system_driver_details === nothing\n        @debug \"Failed to load system driver.\"\n        return\n    end\n    system_driver_version = system_driver_details[1]::VersionNumber\n    device_capabilities = system_driver_details[2]::Vector{VersionNumber}\n    @debug \"System driver version: $system_driver_version\"\n\n    # determine if loading the forwards-compatible driver would exclude devices\n    for (dev, cap) in enumerate(device_capabilities)\n        # CUDA 12 deprecated Kepler\n        if compat_driver_version >= v\"12\" && system_driver_version < v\"12\" && v\"3.0\" <= cap <= v\"3.5\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n\n        # CUDA 13 deprecated Maxwell, Pascal, and Volta\n        if compat_driver_version >= v\"13\" && system_driver_version < v\"13\" && v\"5.0\" <= cap <= v\"7.2\"\n            @debug \"Loading forwards-compatible driver would exclude device $dev with capability $cap\"\n            return\n        end\n    end\n\n    # finally, load the forwards-compatible driver\n    @debug \"Using forwards-compatible CUDA driver.\"\n    global libcuda = libcuda_compat\n\n    # load the driver and its dependencies; this should now always succeed\n    # as we've already verified that we can load it in a separate process.\n    for dep in libcuda_deps\n        Libdl.dlopen(dep; throw_error=true)\n    end\n    Libdl.dlopen(libcuda_compat; throw_error=true)\n"""
        julia_compat = "1.10"
        lazy_artifacts = false
        name = "CUDA_Driver"
        platforms = ["x86_64-linux-gnu", "aarch64-linux-gnu"]
        script = """    # Build the driver inspection binary\n    mkdir -p ${bindir}\n    ${CC} -std=c99 -O2 cuda_inspect_driver.c -ldl -o ${bindir}/cuda_inspect_driver\n\n    mkdir -p ${libdir}\n\n    cd ${WORKSPACE}/srcdir/cuda_compat*\n\n    install_license LICENSE\n\n    mv compat/* ${libdir}\n"""
        version = "v13.1.1"

            [["13.1.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcuda"]
            type = "lib"
            variable_name = "libcuda_compat"
            [["13.1.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libcudadebugger"]
            type = "lib"
            variable_name = "libcuda_debugger"
            [["13.1.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-gpucomp"]
            type = "lib"
            variable_name = "libnvidia_gpucomp"
            [["13.1.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-nvvm"]
            type = "lib"
            variable_name = "libnvidia_nvvm"
            [["13.1.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-ptxjitcompiler"]
            type = "lib"
            variable_name = "libnvidia_ptxjitcompiler"
            [["13.1.1+0".artifact_metadata.metadata.products]]
            dir_paths = []
            dlopen_flags = []
            dont_dlopen = true
            libnames = ["libnvidia-tileiras"]
            type = "lib"
            variable_name = "libnvidia_tileiras"
            [["13.1.1+0".artifact_metadata.metadata.products]]
            binnames = ["cuda_inspect_driver"]
            type = "exe"
            variable_name = "cuda_inspect_driver"

            [["13.1.1+0".artifact_metadata.metadata.sources]]
            hash = "0e8c1ec14c2abbce76943cb32907b39237a41bf267ec6665f04d48b240ae539f"
            type = "archive"
            unpack_target = ""
            url = "https://developer.download.nvidia.com/compute/nvidia-driver/redist/cuda_compat/linux-x86_64/cuda_compat-linux-x86_64-590.48.01_cuda13.1-archive.tar.xz"
            [["13.1.1+0".artifact_metadata.metadata.sources]]
            follow_symlinks = false
            path = "./src"
            target = ""
            type = "directory"
            [["13.1.1+0".artifact_metadata.metadata.sources]]
            hash = "f3e02edc1a1080f44ec69478011e73218c7e20eaf52422963fd084ad0310bb64"
            type = "archive"
            unpack_target = ""
            url = "https://developer.download.nvidia.com/compute/nvidia-driver/redist/cuda_compat/linux-sbsa/cuda_compat-linux-sbsa-590.48.01_cuda13.1-archive.tar.xz"
